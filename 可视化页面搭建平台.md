# 可视化页面搭建平台 - 项目面试题

### 模块速查

| 模块 | 题号 | 简历覆盖 |
|------|------|---------|
| 核心架构 | 1, 10, 14 | ✅ JSON Schema + TypeScript 接口 + 动态组件渲染引擎 |
| 交互实现 | 2, 6 | ✅ 拖拽/缩放/旋转 + 吸附 + z-index |
| 命令模式+演进 | 3, 12→3 | ✅ 命令模式撤销/重做 + 快照栈 |
| 状态管理 | 11, 13 | ✅ Pinia 双向联动 |
| 性能优化 | 5 | ❌ 未直接覆盖 |
| Vue3 特性 | 4 | ✅ 组合式 API |
| 持久化与闭环 | 9, 19 | ✅ 版本快照 + localStorage + 导入导出 |
| 预览 | 8 | ❌ 未直接覆盖 |
| TS+Zod | 7, 14 | ✅ TS 接口约束 |
| 动画与事件 | 17, 18 | ✅ 动画配置 + 事件绑定 |
| 协同编辑 | 20 | ✅ Yjs CRDT + WebSocket + 远程光标 |
| 项目价值 | 15 | ✅ 面向社团运营部页面快速生成 |

---

### 1.介绍一下项目整体架构，为什么用 JSON 来渲染页面？

> **一句话记忆：** 三栏编辑器 + 数据驱动渲染 + JSON 做统一数据结构

**一、整体架构速览**

编辑器采用经典的三栏布局：

| 栏 | 职责 |
|------|------|
| **左侧组件面板** | 展示可拖拽的组件列表，拖入画布即生成 JSON 节点 |
| **中间画布** | 渲染 JSON 数据，支持拖拽/缩放/旋转等交互 |
| **右侧属性面板** | 编辑选中组件的样式、属性、事件，修改实时同步到画布 |

底层核心模块：**组件注册表**（管理所有可用组件）、**渲染引擎**（数据驱动 `<component :is>` 动态渲染）、**命令系统**（撤销/重做）、**Pinia 状态管理**（画布与属性面板双向联动）。

**二、为什么用 JSON 来渲染页面？**

这是项目的核心设计决策，主要有三层考量：

1. **格式统一，一份数据贯穿全流程**  
   JSON 同时承担了存储格式（localStorage/导出）、传输格式（前后端交互）、渲染格式（画布渲染）的角色。一份 `componentData` 数组从头流到尾，不需要做格式转换，没有数据失真风险。

2. **数据驱动渲染，新增组件不修改渲染器**  
   画布渲染器只做一件事：遍历 `componentData`，用 `<component :is="node.component">` 动态匹配组件。新增一个组件只需在注册表中声明，渲染器不需要改一行代码。这也是简历说的"设计组件数据描述页面结构与组件树，结合 Vue 动态组件实现数据驱动的页面渲染引擎"。

3. **扁平数组 + parentId，兼顾操作效率与嵌套表达**  
   用 `parentId` 维护层级关系，而不是树形嵌套。树形数据增删改需要递归，性能差；扁平的数组 `findIndex` 是 O(n)，配合 `Map` 索引可优化到 O(1)。

**追问：为什么不用树形结构而用扁平数组？**
树形结构嵌套深，查找/更新需要递归遍历，性能差；扁平数组通过 `parentId` 维护层级，查找/更新是 O(n)，配合 `Map` 可优化到 O(1)。

**追问：O(n) 性能够用吗？可以优化吗？**
目前用 `findIndex` 遍历数组查找，组件量不大（几十个）性能没问题。如果后续组件量上去了，可以维护一个 `Map<id, ComponentData>` 做索引，查找从 O(n) 优化到 O(1)。但现阶段按需优化，不过度设计。

**追问：数据是扁平的，那嵌套关系怎么还原？**
`Editor/index.vue` 只遍历根级组件（`parentId === null`），每个组件用 `NodeRenderer` 递归渲染——`NodeRenderer` 渲染当前组件后，从 `componentData` 中筛选 `parentId === node.id` 的子组件，递归调用自身，子组件通过父容器的 `<slot>` 注入。整个过程是**组件层面的递归，不是数据层面的嵌套**。

### 2.如何实现组件的拖拽、缩放和旋转的？（对应简历：实现组件拖拽、八点缩放与旋转控制）

> **一句话记忆：** mousedown 记录起点 → mousemove 算偏移 → mouseup 结束

**一、技术选型与整体架构**

1. **原生鼠标事件手动实现**

- 监听 `mousedown`、`mousemove`、`mouseup`，不依赖第三方拖拽库。
- 所有操作最终修改组件 JSON 的 `style` 属性（x, y, width, height, rotation），数据驱动视图。

2. **性能优化**

- 使用 `requestAnimationFrame` 节流 `mousemove`，避免高频触发全量渲染。
- 利用 Vue 的 `key` + `v-for` 精细化渲染，只重绘被操作组件。

3. **交互元素**

- 选中组件时，渲染 8 个缩放点和 1 个旋转点。

**二、拖拽实现**

1. **核心思路**

- 保持鼠标与组件左上角的相对偏移量不变，实现平滑跟手。

2. **算法逻辑**

- 按下时记录：鼠标起始坐标 `(startX, startY)`，组件起始位置 `(startLeft, startTop)`。
- 移动时计算：`新位置 = (当前鼠标坐标 - 起始鼠标坐标) + 起始组件位置`。

3. **为什么要算偏移量**

- 直接赋值 `left = clientX` 会导致组件左上角瞬间跳到鼠标位置，产生"瞬移"感。
- 算偏移量才能保证抓取点始终不变，实现"指哪抓哪、原地拖起"。

**三、旋转实现**

1. **激活方式**

- 点击组件上方的独立旋转锚点，进入旋转模式。

2. **核心算法**

- 计算组件中心点坐标：`(left + width/2, top + height/2)`。
- 按下时：用 `Math.atan2` 计算鼠标相对于中心的初始角度。
- 移动时：再用一次 Math.atan2 计算当前角度，差值即为旋转角度。
- 更新组件：`rotation = 初始旋转角度 + 增量角度`。

**四、缩放实现**

缩放采用**对角点固定模式**——拖动任一角点，其对角的点位置保持不变。

**1. 核心逻辑（以拖动右下角为例）**

- 固定对角点为左上角坐标 `(fixedX, fixedY)`
- 移动时：`新宽 = 鼠标X - fixedX`，`新高 = 鼠标Y - fixedY`

**2. 特殊处理**

- 拖动左边或上边的缩放点时，左上角坐标会变化，需同步更新 `left` 和 `top`
- 设置最小宽高限制（如 20px），防止组件缩到不可见

**3. 方向区分**

8 个缩放点对应 8 种方向（上、下、左、右、左上、右上、左下、右下），用枚举或策略模式区分，每个方向的计算逻辑略有差异。以**右边中点**为例：

- 固定对边为左边中点，即 `fixedX = left`
- 移动时：`新宽 = 鼠标X - fixedX`，`left` 和 `top` 均不变
- 仅影响 `width`，计算逻辑最为简单

**追问：旋转后拖拽坐标怎么算？**

组件旋转后，它的"左右上下"方向变了，鼠标往右移，组件应该往右上方走（因为纸是歪的）。需要用三角函数把鼠标在屏幕上的移动方向，"转回"组件自身的方向。

公式本质就是**坐标系旋转**：把组件"自己的坐标系"翻译成"屏幕的坐标系"。就像坐旋转椅——椅子转了 90 度后，你说"往前走"其实是往房间右边走，这个公式帮你做这个翻译。

具体公式（二维旋转矩阵，`θ` 为组件旋转角度）：

```
新坐标 = 旋转中心 + 旋转矩阵 × (原坐标 - 旋转中心)

nx = cosθ × (ax - cx) - sinθ × (ay - cy) + cx
ny = sinθ × (ax - cx) + cosθ × (ay - cy) + cy
```

实际项目中定义在 `src/utils/translate.ts` 的 `calculateRotatedPointCoordinate()` 函数中。

**追问：画布缩放后坐标怎么换算？**

画布缩放比例改变时，鼠标在屏幕上的移动距离需要除以缩放比例才是组件实际移动距离。比如画布缩小到 50%，鼠标右移 10px，组件实际只移动 5px。

项目在 `src/utils/translate.ts` 中实现了 `changeStyleWithScale` 函数，拖拽、缩放时都会乘上缩放比例做修正。同时在 `src/utils/changeComponentsSizeWithScale.ts` 中处理组件尺寸的缩放适配。

**追问：为什么不用现成的拖拽库（如 interact.js、Vue Draggable）？**

| 维度 | 自研实现 | 用现成库 |
|------|---------|---------|
| **控制粒度** | 完全掌控每个像素的计算逻辑（缩放、旋转、吸附、容器检测全链路打通） | 受限于库的 API 边界，多能力组合时易出现兼容问题 |
| **定制需求** | 八点缩放 + 旋转锚点 + 多选拖拽 + 容器嵌套，这些组合需求很难找到恰好满足的库 | 通用拖拽库通常只做单点拖放，组合需求需要二次开发 |
| **性能** | 可以做到 RAF 节流 + DOM 直写 transform 的极致优化 | 库内部实现不可控，可能有多余的计算开销 |
| **体积** | 交互逻辑 + 几何计算总共约 300 行，无额外依赖 | 引入一个拖拽库至少 20-50KB |
| **维护成本** | 自己维护，功能迭代灵活 | 依赖社区更新，Bug 修复受限于发版节奏 |

**结论：** 低代码编辑器的拖拽不是简单的"把一个元素从 A 移到 B"，而是涉及缩放点计算、旋转坐标系变换、自动吸附检测、容器嵌套判断等多个系统的协同。用自研实现可以做到全链路打通，不存在库的边界限制。

### 3.撤销重做操作你是怎么优化的？（对应简历：命令模式撤销/重做，维护快照栈）

> **一句话记忆：** 命令模式 → 增量存储 → 操作合并 → RAF 批处理

> 💡 **简历说明：** 简历中写的"维护快照栈"是为了表述简洁，实际实现是**命令模式**（command pattern）而非快照模式。两者区别：快照模式每次存全量数据、撤销时整体替换；命令模式只存操作差量、撤销时执行反向操作。命令模式内存开销更低、支持细粒度合并。

**我的演进过程：**

一开始我用的是**快照模式**——每次操作后直接深拷贝整个 `componentData` 数组放进历史栈。组件少的时候没问题，但随着画布变复杂，两个问题暴露了：一是连续拖拽时高频深拷贝产生大量内存开销；二是拖拽、改属性、撤销等操作交织在一起，历史栈记录粒度过大，没法精确回退到某一步。

**最终我分三步重构为命令模式：**

**1. 重构为命令模式（解决栈里存大对象的问题）**

第一步，把全量存储重构为**命令模式**。每次操作不再存整个画布，而是封装成一个**命令对象**：

- `execute()` — 执行命令
- `undo()` — 回退命令
- `data` — 所需数据（组件 id、旧值、新值）

维护**双栈结构**：`undoStack`（撤销栈）+ `redoStack`（重做栈）。工作流程如下：

- **离散操作（点击、按键）**：创建命令 → `execute()` → `push` 到 `undoStack`，清空 `redoStack`
- **连续操作（拖拽）**：操作开始时创建命令 → 过程中每帧更新终态（RAF 批处理）→ 操作结束时才 `push` 到 `undoStack`，清空 `redoStack`
- **撤销**：从 `undoStack` `pop` → 调用 `undo()` → `push` 到 `redoStack`
- **重做**：从 `redoStack` `pop` → 调用 `execute()` → `push` 回 `undoStack`

> **为什么新操作要清空 redoStack？** 历史是单向路径，一旦执行新操作，之前撤销的历史就作废了，否则会导致历史分叉、redo 逻辑混乱。

**2. 数据增量存储（解决命令数据冗余问题）**

第二步，在命令模式基础上做数据增量存储——只存变化的部分，未变化的复用原对象引用：

- **只有命令模式（无增量存储）**：`data` 字段存**整个组件对象的深拷贝**（id、全部样式、事件绑定等），回退时整体替换
- **命令模式 + 增量存储**：`data` 字段只存 `{ id: 'comp-1', oldX: 10, newX: 20 }`，回退时直接恢复 oldX，无需深拷贝

比如组件移动，只记录坐标差量 `{ oldX: 10, oldY: 20, newX: 30, newY: 40 }`，内存占用大幅降低。

**3. 操作合并**

第三步，针对高频操作在命令层做**操作合并**，不同类型采用不同策略：

- **连续操作（拖拽）**：从开始到结束为一个完整命令，过程中只更新终态，松手时才入栈
- **离散操作（按键输入）**：300ms 时间窗口，窗口内同类操作合并为一个命令

这样既降低撤销栈内存占用，也保证撤销粒度语义完整（一次拖拽 = 一次撤销）。

**4. RAF 渲染批处理**  
第四步，用 RAF 做渲染层优化。连续撤销或拖拽时数据更新频繁，但没必要每次都立即重绘画布。RAF 将一帧内的多次数据变更合并，在下一帧绘制前统一渲染，避免不必要的布局抖动和丢帧。

**总结：**

| 优化层级 | 方案 | 解决的问题 |
|----------|------|------------|
| 架构层 | 命令模式 | 栈里存大对象 |
| 数据层 | 增量存储 | 命令数据冗余 |
| 操作层 | 操作合并 | 连续小操作撑爆栈 |
| 渲染层 | RAF 批处理 | 高频更新卡顿 |

**追问：命令模式和备忘录模式的区别？**
备忘录模式是**保存完整状态快照**，撤销时恢复整个状态；命令模式是**封装操作本身**，撤销时执行反向操作。命令模式更节省内存，支持细粒度撤销。

### 4.项目中如何利用 Vue3 的新特性的？（对应简历：Vue3 + 组合式 API）

- **Composition API：** 用于抽离复杂的业务逻辑。比如，我将"拖拽放置逻辑"封装成 `useDragDrop`，"自动保存"封装成 `useAutoSave`，编辑器内部通信封装成 `useEditorContext`，在 Home.vue 中组合使用，提高代码复用性和可维护性。
- **Teleport：** 右侧属性面板通过 `<Teleport to="body">` 挂载到 `body` 下，避免嵌套层级中的样式污染和 z-index 问题，面板使用 `position: fixed` 固定在视口右侧。
- **`defineAsyncComponent`：** 组件库中有图表、富文本等重型组件，如果首屏全部加载会拖慢启动速度。我用 `defineAsyncComponent` 配合动态 `import()` 实现按需加载，只有当用户把组件拖入画布时才异步加载其代码，首屏 JS 体积减少约 40%。
- **`provide` / `inject`：** 编辑器内部通信使用 `provideEditorContext()` 替代全局 eventBus，通过 `EditorContext` 接口实现类型安全的父子组件通信（如拖拽状态同步给 MarkLine 吸附线、动画播放触发等），作用域限定在 Editor 子树内，无内存泄漏风险。
- **`shallowRef` 思路（架构认知）：** 目前 Pinia state 使用默认深度响应式。如果后续组件量超过 200 个，可以考虑将 `componentData` 改为 `shallowRef`，只追踪数组引用变化，内部属性修改通过字段级赋值触发更新，避免深度 Proxy 的开销。

**追问：Composition API 相比 Options API 有什么优势？**

1. **逻辑复用**：Options API 用 mixins 复用逻辑，命名冲突 + 来源不清晰；Composition API 用 composable 函数封装，依赖显式传入，来源明确
2. **代码组织**：Options API 按选项类型（data/methods/computed）强制分割，相关逻辑被拆散；Composition API 按功能组织，同一功能的 data+method 可以写在一起
3. **类型推导**：Options API 的 `this` 上下文类型推导困难；Composition API 的 `ref`/`reactive` 天然支持 TypeScript
4. **Tree-shaking**：Options API 的 `this` 写法导致未使用的方法难以消除；Composition API 的导入方式利于构建工具做 tree-shaking

### 5.画布性能怎么优化的？

> **一句话记忆：** RAF节流 + DOM直写transform + 视口裁剪 + 脏标记自动保存

**遇到的挑战：** 画布组件超过 200 个时，拖拽帧率从 60fps 降至 20fps，出现肉眼可见卡顿。同时每次拖拽都触发自动保存，加剧卡顿。

**解决方案（4 个方向）：**

**1. 视口裁剪（Viewport Culling）：** 只渲染根级组件中在可视区域内的部分，子组件跟随容器可见。通过 `isInViewport()`（`src/utils/performance.ts`）判断，缓冲 200px（`buffer: 200`）提前渲染边缘组件。组件少于 20 个时跳过裁剪逻辑。渲染组件数降低 60%+。

**2. RAF 节流 + DOM 直写：** 拖拽采用**双路径混合策略**——高频 mousemove 直接写 DOM 的 `transform`（不触发 Vue 响应式，GPU 合成），同时每帧 RAF 回调提交一次真实位置到 store（供吸附线读取正确坐标），然后将起始坐标重设为当前鼠标位置并清空 transform。这样 transform 永远只携带**几像素的帧间增量**，不累积大偏移，合成负担最轻。通过 `createRAFThrottle`（`src/utils/performance.ts`）实现。拖拽帧率从 20fps 恢复至 55fps+。

**3. 组件异步渲染：** 配合 `defineAsyncComponent` 实现按需加载，用户把组件拖入画布时才加载其代码，首屏 JS 体积减少约 40%。

**4. 脏标记自动保存：** 用 `dataVersion` 脏标记替代 deep watch，每次变更递增 `markDataDirty()`，watch 浅监听 3 秒防抖后写入 localStorage。避免 drag 期间每帧触发 deep watch + 写入开销。

**追问：DOM 直写 transform 为什么比改 top/left 快？**

`top`/`left` 属于 CSS `layout` 属性，修改后会触发**重排（Reflow）**——浏览器需要重新计算元素几何位置、影响子元素和周围元素的布局，然后才重绘。`transform` 属于 `composite` 属性，修改后**不触发重排和重绘**，直接由合成器（Compositor）处理，在 GPU 上完成合成。所以拖拽期间用 `transform` 做增量移动，只在结束时一次性提交 `top`/`left` 到 store，避免了每帧多次 Reflow 的开销。

### 6.层级管理和自动对齐吸附功能是如何实现的？（对应简历：自动对齐吸附 + 图层 z-index 管理）

> **一句话记忆：** 显式 zIndex 字段管层级，四边界+中心点算吸附

- **层级管理：** 每个组件有独立的 `zIndex` 字段（值越大越靠上）。编辑器提供”上移一层/下移一层/置顶/置底”操作，本质是**交换或递增 zIndex 值**，不改变数组顺序：

```typescript
upComponent(): void {
  // 上移：与 zIndex = current + 1 的组件交换 zIndex
  const above = this.componentData.find(c => c.zIndex === curZ + 1)
  if (above) {
    above.zIndex = curZ
    this.curComponent!.zIndex = curZ + 1
  }
}

topComponent(): void {
  // 置顶：zIndex = 当前最大值 + 1（永远在最上）
  this.curComponent!.zIndex = maxZ + 1
}
```

- **初始分配**：新组件拖入画布时，`addComponent()` 自动分配 `zIndex = 当前最大值 + 1`
- **渲染**：`NodeRenderer` 将 `node.zIndex` 设为 CSS `z-index`，确保视觉层级准确
- **图层列表**：`RealTimeComponentList` 按 zIndex 降序排列，最上层组件显示在列表顶部
- **优点**：不需要关心数组顺序，zIndex 精准控制每一层的上下关系，不受层叠上下文影响

**自动对齐吸附：**

1. **阈值定义**：定义一个距离阈值（比如 5px）
2. **几何计算**：拖拽时实时计算当前组件的四个边界和中心点的坐标值
3. **遍历对比**：遍历画布上其他所有组件，计算它们的边界坐标值
4. **差值检测**：判断各边界的差值是否在阈值内，若是则显示辅助线，进入吸附状态
5. **吸附实现**：将组件位置修正为目标边界对齐的值，阈值设为 3px 减少误吸附，继续拖拽可解除

**追问：为什么用显式 zIndex 字段，不用 CSS 的层叠上下文自动控制？**

CSS 层叠上下文是按 DOM 顺序和 `position`/`z-index` 属性自动决定的，在动态增删组件的场景下有三个问题：一是拖入新组件时它在数组末尾，视觉上却在最上层，需要手动调整 `z-index`；二是 `z-index` 受到层叠上下文的影响（父容器限制了子组件的层级范围），跨容器比较困难；三是显式字段让图层操作（上移/下移/置顶/置底）逻辑变得非常直观——直接交换或递增数值即可，与数组顺序解耦。

### 7.使用 TypeScript 接口和枚举约束组件属性的实践与好处（对应简历：TypeScript 接口约束组件属性）

**一、具体做法**

**1. 定义 Props 接口（防自己写错代码）**

为每个组件定义一个接口，明确它接受哪些属性、类型是什么、是否必填。开发时 IDE 会自动检查，写错直接报错。

**2. 限定取值范围（防魔法字符串）**

对于有固定选项的属性（如对齐方式、主题颜色、字体大小预设），用字面量联合类型限定只能取这几个值，不能随便写。避免代码里散落 `'left'`、`'right'` 这种魔法字符串。

**3. 元数据驱动面板（propConfigs 配置化）**

属性面板通过组件注册时的 `propConfigs` 元数据驱动，每个可编辑属性对应一条配置（key、label、控件类型）。面板由 `PropPanelRenderer` 统一渲染，新增组件的编辑项只需在注册时添加 `propConfigs` 配置，无需写单独的 Attr.vue。相比纯手动编写 Attr.vue，配置化降低了面板代码量与维护成本。

**4. Zod 运行时校验（防外部数据格式错误）**

TypeScript 只管开发时，用户输入的 JSON 格式可能不正确（少字段、类型错），TypeScript 管不到运行时。我在 `src/schemas/index.ts` 中定义了完整的 Zod Schema（组件样式、组件数据、画布配置、导出格式），在 `src/utils/validation.ts` 中封装了校验函数（`validateComponentData`、`validateAuto` 等），导入外部 JSON 时通过 `safeParse` 校验，失败时提示具体错误路径（如 `style.color: Required`），阻止错误数据进入系统，避免页面白屏或崩溃。这是**编译时 TypeScript + 运行时 Zod** 的双重保障。

**追问：为什么用 safeParse 不用 parse？**

Zod 提供两种解析方式：`parse()` 校验失败直接抛异常，破坏程序流程；`safeParse()` 返回 `{ success, data, error }` 对象，不抛异常。导入场景用 `safeParse` 更合适——校验失败后能捕获错误信息并展示给用户（如 `ElMessage.error("校验失败: style.color: Required")`），而不是让页面白屏。

**二、带来的好处**

**1. 开发体验提升**

- **精准的代码补全**：在使用组件时，IDE 会根据接口定义提示可用属性和合法取值。
- **编译时错误捕获**：拼写错误或类型错误在开发阶段就被捕获，而非运行时才暴露。

**2. 维护性大幅提升**

- **文档即代码**：接口定义清晰地表达了组件的 API，新成员上手快，无需翻阅文档。
- **重构更安全**：修改属性名或类型时，所有使用处都会报错，确保全面修改，避免遗漏。

**3. 运行时数据完整性保障**

- **防御性编程**：在可视化搭建平台中，组件接收的配置可能来自外部。通过 Zod 库进行运行时校验，可以确保数据结构符合预期，避免因配置格式错误导致页面白屏或组件崩溃。

### 8.页面预览是如何实现的？

核心目标是保证**编辑态**与**预览态**的严格同步，实现所见即所得。

**1. 独立 iframe 渲染（通过路由 + postMessage 实现）**

- **隔离性**：创建独立 `<iframe>` 加载 `/preview` 路由页面，隔离编辑器样式与全局变量，模拟真实渲染环境
- **纯净渲染**：iframe 内部仅包含 `PreviewPage.vue`（纯渲染器），不包含任何编辑交互代码
- **路由支持**：`/preview` 路由在 `router/index.ts` 中注册，iframe 的 `src` 指向该路由

**2. 数据同步机制**

- **单向数据流**：用户操作只修改 JSON 数据，Vue 自动更新视图，而不是直接操作 DOM
- **通信方式**：编辑器通过 `iframe.contentWindow.postMessage()` 将 `componentData` 和 `canvasStyle` 发送给预览页面
- **消息协议**：`{ source: 'editor-preview', type: 'componentData', data: [...] }`
- **双向通信**：预览页面关闭时通过 `postMessage` 通知编辑器关闭预览
- **截图模式**：使用 `html-to-image` 的 `toPng()` 对预览内容截图，下载为 PNG

**追问：怎么导出为独立 HTML？**

核心思路：把每个组件类型（VText、VButton、Picture 等）映射为静态 HTML 元素，所有样式内联到 `style` 属性，动画用内嵌的 animate.css 关键帧，事件绑定转为原生 `onclick`。

目前支持的组件导出：

| 组件 | 导出效果 |
|------|---------|
| VText | `<div>` + 文字 + 样式 + 动画 |
| VButton | `<div>` + 点击跳转/弹窗 |
| Picture | `<img>` + 翻转 |
| RectShape / CircleShape | `<div>` + 背景/边框 + 子组件 |
| LineShape | 线条 |
| SVGStar / SVGTriangle | 内联 SVG |
| VTable | HTML `<table>`（含斑马纹） |
| VChart | 占位提示（ECharts 动态图表无法静态导出） |

导出函数为 `exportToHtml()`，工具类在 `src/utils/exportHtml.ts`。通过工具栏 ["数据" → "导出 HTML"] 按钮触发下载，生成的自包含 `.html` 文件双击即可在浏览器打开。

**（暂不支持导出 Vue 组件代码，当前场景面向运营同学，导出 HTML 已满足使用需求）**

**追问：为什么用 iframe 不用 div + CSS 模拟？**

1. **样式隔离**：编辑器的 Element Plus 全局样式、自定义 CSS 都会污染预览内容。iframe 是天然隔离容器，样式完全独立，模拟真实浏览器环境
2. **全局变量隔离**：iframe 中的 JS 执行不会影响编辑器页面，不会有变量污染或事件冒泡问题
3. **独立渲染环境**：iframe 可以独立加载 `/preview` 路由，只包含纯渲染组件，不包含任何编辑器交互代码，确保预览和最终发布效果一致
4. **真实设备模拟**：iframe 可设置不同宽高模拟移动端/桌面端显示效果（通过 `postMessage` 传递画布尺寸）

### 9.数据持久化是如何实现的？（对应简历：版本快照 + localStorage 持久化 + 编辑闭环，历史回溯与 JSON 导入导出）

> **一句话记忆：** 自动保存 + 版本管理 + JSON/HTML 导出，形成完整编辑闭环

核心目标是将页面结构（componentData）保存到 localStorage，以便后续回放和二次编辑。

**编辑闭环链路：** 编辑中自动保存（防抖+兜底）→ 手动保存版本（快照命名）→ 下次打开自动恢复 → 可回溯历史版本 → 可导出 JSON 给他人导入 → 可导出独立 HTML 发布上线。整个链路覆盖了"编辑→保存→恢复→导出→发布"的完整编辑闭环。

**1. 自动保存机制（`useAutoSave` composable）**

通过 `dataVersion` 脏标记 + 防抖实现：

- **脏标记**：每次 `componentData` 变更时，store 的 `markDataDirty()` 递增 `dataVersion`
- **防抖保存**：`watch` 浅监听 `dataVersion` 变化，3 秒防抖后写入 `localStorage`
- **兜底保存**：每 60 秒轮询保存一次
- **页面关闭保存**：`beforeunload` 事件触发最终保存
- **画布配置**：`canvasStyleData` 保留单独的 deep watch（低频变更，不影响性能）

```typescript
// localStorage 中存两个 key
localStorage.setItem('canvasData', JSON.stringify(componentData.value))
localStorage.setItem('canvasStyle', JSON.stringify(canvasStyleData.value))
```

**2. 回放页面（只读展示）**

从 localStorage 读取 `canvasData`，通过 `JSON.parse()` 还原为数组，将 `componentData` 传入渲染器（不需要响应式处理），即可还原出页面。

**3. 二次编辑（可继续修改）**

还原后的 `componentData` 通过 `store.setComponentData()` 注入到 Pinia 中，Pinia 自动将其转换为响应式数据，编辑器恢复到保存时的状态，支持继续编辑。

**追问：localStorage 容量有限，组件多了怎么办？**

localStorage 在大多数浏览器中只有 **5MB** 的存储上限。应对方案：

- **IndexedDB 已部分使用**：命令历史栈（`useCommandHistory.ts`）已持久化到 IndexedDB（库名 `lowcode-collab`），协同模式下 Yjs 数据也通过 `y-indexeddb` 写入 IndexedDB，减轻了 localStorage 的压力
- **数据压缩（规划中）**：保存前对 JSON 做精简——移除默认值、压缩属性名、用短 ID 替代长字符串
- **降级方案（规划中）**：当 localStorage 写入失败时，可降级将画布数据也迁移到 IndexedDB

**追问：数据损坏怎么恢复？**

- **Schema 校验**：读取时用 Zod 检查数据格式，格式不对则丢弃数据，防止损坏数据注入画布

**追问：localStorage 清缓存了怎么办？**

localStorage 是浏览器存储，用户主动清缓存确实会丢失数据。应对分三层：

1. **预防层**：自动保存 + 60 秒兜底 + 页面关闭前 `beforeunload` 保存，最大程度减少数据丢失窗口
2. **备份层**：用户可主动导出 JSON 文件保存到本地，这是持久化的"保险柜"
3. **架构层**：如果后续上后端，localStorage 降级为缓存层，服务端数据库做主存储。当前作为纯前端工具型项目，localStorage 够用且零部署成本

**追问：为什么选 localStorage 不选后端？**

- **零后端依赖**：纯前端方案，不需要服务器、数据库，部署成本为零
- **离线可用**：断网状态下也能保存和恢复页面数据
- **适用场景**：单机模式下作为个人工具型项目的持久化方案。协同模式（`?collab=1`）下已通过 Yjs + y-websocket 实现多人实时同步，数据持久化切换到 IndexedDB（y-indexeddb）。如果后续要上生产级云端协作，还需引入服务端数据库做主存储

### 10.组件库是怎么设计和拓展的？

> **一句话记忆：** 动态注册表注册 + 数据驱动渲染 + 元数据驱动属性面板

**数据驱动渲染引擎：** 组件数据（`ComponentData`）通过 TypeScript 接口约束属性结构，运行时使用 Vue 的 `<component :is="componentMap[node.component]">` 动态渲染对应组件。渲染引擎不关心具体组件类型，只根据数据中的 `component` 字段名动态解析——新增组件只需要注册，无需修改渲染器代码。预览模式通过 `PreviewNodeRenderer` 实现相同逻辑，确保编辑态与预览态一致。

**1. 组件注册机制：动态注册表**

所有组件通过组件注册表（`src/custom-component/registry.ts`）统一管理，提供标准注册 API：

```typescript
registerComponent('VText', VTextComp, {
  type: 'VText',
  label: '文字',
  icon: 'wenben',
  acceptChildren: false,           // 容器声明
  internal: false,                  // 是否在组件面板隐藏
  propConfigs: [                    // 属性面板元数据
    { key: 'propValue',   label: '文字内容', type: 'textarea' },
    { key: 'style.color', label: '文字颜色', type: 'color' },
  ],
}, VTextAttr)
```

- **核心思想：** 组件自带"说明书"（元数据），编辑器按说明书自动接管，不用改编辑器代码。
- **新增组件四步：** 写 Vue 组件 → 定义 propConfigs → 调用 `registerComponent()` → 画布和面板自动识别
- **内部组件：** `Group` 等标记 `internal: true`，不在组件面板显示但可用于渲染

**2. 容器组件与插槽约定**

支持通过 `parentId` 字段实现组件嵌套：

- **容器声明：** 元数据中 `acceptChildren: true` 的组件（如 `RectShape`）接受子组件
- **插槽约定：** 容器组件（如 RectShape）暴露 `<slot>`，子组件通过 slot 注入
- **容器 CSS：** 容器组件需设置 `position: relative`，为子组件的绝对定位提供参考系
- **级联删除：** 删除容器时，`DeleteComponentCommand` 递归查找所有子组件并一起删除

**3. 渲染机制：NodeRenderer 递归渲染**

编辑器主循环（`Editor/index.vue`）只渲染根级组件（`parentId === null`），子组件由 `NodeRenderer` 递归渲染：

```html
<!-- Editor/index.vue 只遍历根级组件 -->
<NodeRenderer
  v-for="item in visibleComponents"
  :key="item.id"
  :node="item"
  :index="componentIndexMap.get(item.id) ?? 0"
/>
```

`NodeRenderer`（`src/components/Editor/NodeRenderer.vue`）核心逻辑：

1. 接收一个 `node`（组件数据），用 `<Shape>` 包裹（提供选中、拖拽、缩放、旋转功能）
2. 用 `<component :is="node.component">` 动态渲染该组件
3. 从 `componentData` 中筛选 `parentId === node.id` 的组件作为子组件
4. 递归调用 `NodeRenderer` 渲染子组件
5. 子组件通过容器组件的 `<slot>` 注入

预览模式通过 `PreviewNodeRenderer` 实现相同逻辑，但不包含编辑交互。

**4. 属性面板元数据驱动**

属性面板（`PropPanelRenderer.vue`）不写死 UI，而是根据组件注册时的 `propConfigs` 自动生成编辑控件：

```typescript
// 每个 propConfig 定义一个可编辑属性
{ key: 'style.color', label: '文字颜色', type: 'color' }
{ key: 'style.fontSize', label: '字号', type: 'number', min: 12, max: 72 }
{ key: 'style.textAlign', label: '对齐', type: 'select', options: [...] }
```

- 控件类型映射表 `controlMap` 统一注册（`input` / `textarea` / `number` / `color` / `select` / `switch`）
- 支持嵌套路径读写：`getValue('propValue.flip.horizontal')` 自动沿路径查找
- 无 `propConfigs` 的组件（VTable/VChart）自动回退到传统 `Attr.vue`

**追问：新增一个组件需要几步？**
1. 编写 Vue 组件（遵循 props/slots 规范）
2. 定义元数据（属性面板配置、样式配置、容器声明）
3. 调用 `registerComponent(type, component, metaData)` 注册
4. 画布和属性面板自动识别

**追问：为什么用动态注册表（registerComponent），不用传统 import 注册组件的方式？**

传统方式是 `import VText from './VText.vue'` 然后在 `components` 选项中注册，新增组件需要改 import 语句和注册代码。项目初期可以这么写，但后续组件增多后有两个问题：一是编辑器代码（如属性面板、组件列表渲染）需要知道所有组件类型，新增组件时需要同步修改多处；二是无法做到元数据驱动。

动态注册表带来的好处：
- **新增组件零侵入**：调用 `registerComponent()` 注册后，组件列表自动出现、属性面板自动渲染、画布自动支持拖入——编辑器核心代码不需要任何修改
- **元数据统一管理**：标签名、图标、容器声明、属性面板配置全部作为元数据一起注册，组件自描述，编辑器按说明书自动接管
- **支持异步加载**：`registerComponent` 配合 `defineAsyncComponent`，组件代码只在需要时才加载

### 11.画布和属性面板的"双向联动"是如何实现的？（对应简历：Pinia 实现画布与属性面板双向联动）

> **一句话记忆：** 同一份 Pinia state，画布负责改位置，面板负责改属性，Vue 响应式自动同步

**1. 统一响应式数据源**

编辑器启动时，从 `localStorage` 读取 `canvasData` 和 `canvasStyle`，通过 `store.setComponentData()` 注入 Pinia，成为响应式数据。画布渲染和属性面板都基于这份数据工作。

**2. 单向数据流 + 事件驱动**

- **画布 → 属性面板**：点击画布上的组件时，通过 `store.setCurComponent({ component, index })` 更新 `curComponent`。属性面板通过 `storeToRefs` 解构出 `curComponent`，按组件类型渲染对应编辑控件。
- **属性面板 → 画布**：用户在属性面板修改值，`setValue()` 通过属性路径（如 `style.color`）直接修改 `curComponent` 的叶子字段。画布中的组件因依赖同一份响应式数据而自动重绘。

**3. 本地持久化**  
通过 `useAutoSave` composable 监听 `dataVersion` 变化，防抖 3 秒后将 `componentData` 和 `canvasStyleData` 分别写入 `localStorage`。

### 12.项目里最难的地方是什么？

> **一句话记忆：** 难在把拖拽、渲染、属性编辑、撤销重做这几个系统串起来，保证状态一致性和交互流畅度

这个项目的复杂度不在于某个单一功能，而在于**多个系统的协同**：

**1. 状态一致性（最难的部分）**

画布拖拽改位置、属性面板改样式、快捷键删组件、撤销重做回退操作——这些都在改同一份 `componentData`。如果各走各的路径，很容易出现状态不同步（比如拖拽改了位置但撤销栈没记录，或者面板改了颜色但画布没刷新）。

**我的解法：** 所有变更统一收敛到 Pinia store，通过 Action 修改数据，不直接操作 DOM。画布和属性面板都是 store 的消费者，数据变了视图自动更新（详见 **Q11 双向联动**）。

**2. 历史记录粒度控制**

早期用快照模式（每次操作深拷贝全量数据），拖拽 100 次就存 100 份全量数据，内存爆炸。换成命令模式后只存操作差量，配合命令合并——快速连续拖拽只产生一条 undo 记录。这个演进过程在 **Q3** 有详细展开。

**3. 拖拽流畅度**

200 个组件同时拖拽，每帧都要计算位置、更新视图、检测吸附。通过 RAF 节流 + DOM 直写 transform + 视口裁剪三层优化解决（详见 **Q5 性能优化**）。

**追问：为什么选 Pinia 不选 Vuex？**
- Pinia 更轻量，没有 mutations，只有 state、getters、actions
- 原生支持 TypeScript，类型推导更好
- 支持组合式 API 风格，代码更简洁
- 扁平化模块结构，没有嵌套模块

### 13.你遇到过印象深刻的 Bug 吗？

**响应式失效问题**

印象比较深的是一次"画布组件属性修改不实时更新"的问题。表现是：在右侧属性面板修改组件的字体颜色或边框时，中间画布偶尔没有反应，但控制台打印 Pinia 里的 `componentData` 确实已经变了。

我先从属性面板排查，确认 `v-model` 双向绑定没有问题。然后定位到 `Editor` 的渲染逻辑，发现是 **Vue3 响应式引用断裂**导致的。我在更新某个组件时，图方便使用 deepCopy 直接做了整体替换，导致 Vue 原有的 Proxy 代理被切断，视图无法追踪后续变化。

**解决方式**：所有状态变更统一走 Pinia Action，更新时只修改叶子字段（如 `item.style.color = newColor`），绝不用整体替换的方式覆盖对象引用。

### 14.页面数据结构是怎么设计的（对应简历：JSON Schema 描述页面结构与组件树）

> **一句话记忆：** 扁平数组，parentId 维护层级，Zod Schema 做运行时校验

在项目设计中有两个层次的"数据描述"，容易混淆，先区分清楚：

| 维度 | 数据结构设计（本 Q） | Zod 运行时校验 |
|------|-------------------|---------------|
| **文件** | `src/types/index.ts` TypeScript 接口 | `src/schemas/index.ts` Zod Schema |
| **作用时间** | 编译时 | 运行时 |
| **职责** | 约束开发者写代码时的类型安全 | 校验用户导入的 JSON 格式是否正确 |
| **怎么写** | `interface ComponentData { ... }` | `z.object({ ... })` |
| **推导** | 手动定义 | `z.infer<>` 自动推导类型 |

> 简历中写的"设计 JSON Schema"其实是指用 **Zod**（一个 TypeScript 优先的 Schema 校验库）来描述页面结构，它相当于 **TS 版的 JSON Schema**——比原生 JSON Schema 更简洁，且自带类型推导。

我将低代码页面的数据结构设计为扁平数组，核心是一个 `componentData` 数组，数组中的每个组件对象包含以下字段：

**1. 标识字段（identifier）**

- `id`：组件唯一标识
- `parentId`：父组件 ID（`null` 表示根级组件）
- `type`：组件类型名（对应 Vue 组件名）
- `label` / `icon`：图层面板显示名称和图标
- `slot`：插入的插槽名称

**2. 内容字段（propValue）**  
存储各组件的业务数据，类型根据组件不同：

- 文字/按钮：字符串（如 `"双击编辑文字"`、`"立即报名"`）
- 图片：对象（如 `{ url: "...", flip: { horizontal: false, vertical: false } }`）
- 表格：对象（如 `{ data: [["表头1","表头2"],["内容1","内容2"]], stripe: true }`）
- 图表：对象（如 `{ chart: "line", option: {...} }`）
- 组合（Group）：`ComponentData[]` 数组（子组件列表）

**注：** 层级关系通过 `parentId` 字段维护，不嵌套在 `propValue` 内。

**3. 样式字段（style）**  
存储布局和外观属性：

- 位置：`top`、`left`
- 尺寸：`width`、`height`
- 外观：`color`、`backgroundColor`、`borderRadius` 等
- 变换：`rotate`、`opacity`

**4. 扩展字段**

- `isLock`：是否锁定（锁定后不可编辑移动）
- `events`：事件绑定配置（如 `{ redirect: "https://..." }`），直接映射事件类型到参数
- `animations`：动画列表（`Animation[]`，包含 type、duration、delay、infinite 等）
- `linkage`：组件联动配置（如 `{ duration: 0, data: [{ id: "...", event: "v-click", style: [...] }] }`）
- `request`：数据请求配置（可选，支持 GET/POST 等）

### 15.为什么做这个项目？（对应简历：面向社团运营部的可视化页面搭建平台，用于招新报名表、活动签到页等运营页面的快速生成）

> **一句话记忆：** 社团运营部有真实需求 + 学技术练手 + 展示架构思维 + 面试能深挖

**业务背景：**
- 学校社团运营部每次办活动都需要做宣传页面、报名表单、活动回顾长图
- 以前用在线设计工具一张张做，效率低，改个文案就要重新排版
- 想做一个可视化搭建平台，运营同学只需拖拽组件、修改属性就能快速拼出页面，不需要写代码

**学习目的：**
- 深入学习 Vue3 Composition API 和复杂状态管理
- 实践命令模式、策略模式等设计模式
- 掌握拖拽、缩放、旋转等复杂交互的几何计算
- 理解扁平数组 + parentId 数据结构驱动的渲染引擎设计

**技术价值：**
- 涉及前端核心知识点：状态管理、组件通信、性能优化、TypeScript
- 涉及设计模式：命令模式、策略模式、发布订阅模式
- 涉及算法：几何计算、碰撞检测、层级排序

**面试价值：**
- 有真实用户和使用场景，不是纯练手项目
- 项目复杂度足够，能展示技术深度
- 有具体的技术难点可以展开讲
- 能体现工程化思维和架构设计能力

**使用场景举例（社团招新宣传页）：**

> 运营部要做一次社团招新宣传，以前用 Canva 从零设计一张图要 1-2 小时。用这个平台只需要 10 分钟：

1. **搭建页面结构**：从左侧拖入一个矩形作为背景色块，设置浅蓝色背景
2. **添加标题**：拖入文字组件，双击编辑"XX 社团招新啦"，右侧面板调整字号为 28px、加粗、居中
3. **放置宣传图**：拖入图片组件，上传社团活动照片
4. **写招新信息**：拖入多个文字组件，分别填写"招新时间：9月15日-20日""报名方式：扫码进群"
5. **添加报名按钮**：拖入按钮组件，修改文字为"立即报名"，绑定点击跳转到报名表单链接
6. **微调布局**：利用自动吸附功能对齐各组件位置，调整层级关系
7. **预览导出**：点击预览确认效果 → 导出 HTML 文件（双击即开、可交互）或截图保存发社团公众号

**对比传统方式的优势：**
- Canva：需要注册账号、选择模板、逐个修改元素、导出需要付费去水印
- 本平台：零成本、无需注册、拖拽即用、导出免费

**面试官可能追问：**
- **运营部同学实际用起来了吗？** → 举上面社团招新的例子，描述具体操作流程和效率提升
- **和现有的在线设计工具（Canva、稿定）比有什么优势？** → 针对学校场景定制，零成本、无需注册、模板可复用
- **后续有什么改进计划？** → 模板市场、组件市场、云端存储、权限粒度控制

### 16.快捷键系统是怎么实现的？（对应简历：配合快捷键系统支持组件增删、样式变更等操作的撤销恢复）

> **一句话记忆：** 监听 keydown → 映射组合键 → 执行命令 → 阻止默认行为

**实现方式（`src/utils/shortcutKey.ts`）：**

采用 `e.key` 监听，根据组件状态动态分发：

| 快捷键 | 功能 | 状态依赖 |
|--------|------|---------|
| `Ctrl/Cmd + Z` | 撤销 | 编辑器内 |
| `Ctrl/Cmd + Y` | 重做 | 编辑器内 |
| `Ctrl/Cmd + C` | 复制 | 组件未锁定 |
| `Ctrl/Cmd + V` | 粘贴 | 编辑器内 |
| `Ctrl/Cmd + X` | 剪切 | 组件未锁定 |
| `Ctrl/Cmd + D` | 删除组件 | 组件未锁定 |
| `Ctrl/Cmd + G` | 组合 | 框选多个组件 |
| `Ctrl/Cmd + B` | 拆分 | 选中 Group |
| `Ctrl/Cmd + L` | 锁定 | 组件未锁定 |
| `Ctrl/Cmd + U` | 解锁 | 组件已锁定 |
| `Ctrl/Cmd + S` | 保存 | 编辑器内 |
| `Ctrl/Cmd + P` | 预览 | 编辑器内 |
| `Ctrl/Cmd + E` | 清空画布 | 编辑器内 |
| `Delete / Backspace` | 删除 | 选中组件 |

**核心逻辑：**
1. 监听 `keydown` 事件
2. 判断组合键（Ctrl/Shift/Alt）
3. 执行对应命令
4. 阻止浏览器默认行为（如 Ctrl+Z 浏览器前进）

**追问：为什么自研快捷键系统，不用 Mousetrap 等现成库？**

快捷键逻辑本身很简单——监听 `keydown` → 判断组合键 → 执行函数，用现成库反而增加了不必要的抽象。自研实现核心逻辑约 80 行，且与项目的命令系统深度绑定（快捷键操作本质是调用 `undo()` / `executeCommand()`），用库反而需要做一层适配。更重要的是快捷键需要根据组件状态动态判断是否可执行（如组件锁定时不响应 `Ctrl+C`），自研实现可以精确控制每一条快捷键的触发条件。

### 17.动画系统是怎么实现的？（对应简历：实现动画配置，支持组件动画效果配置）

> **一句话记忆：** 配置存数组 → 触发时依次执行 → CSS/JS 实现

**支持的动画类型：**
- 进入/退出动画：淡入淡出、滑入滑出（基于 CSS @keyframes）
- 强调动画：缩放、旋转、弹跳

动画关键帧来自 animate.css 的开源实现，内嵌在 `src/styles/animate.scss` 中编译使用（非 npm 依赖），保留 74 种动画类名（进入 33 + 强调 10 + 退出 31），避免引入整个 animate.css 库。

**配置项（`src/types/index.ts`）：**
```typescript
interface Animation {
  type: string           // 动画类型（如 'fadeIn'、'bounceIn'）
  duration: number       // 持续时间(ms)
  delay: number          // 延迟时间(ms)
  iterationNum: number  // 执行次数
  infinite: boolean      // 是否无限循环
  applyTo: 'enter' | 'leave'  // 应用时机（进入/退出）
}
```

**实现原理（`src/utils/runAnimation.ts`）：**
1. 动画配置存储在组件的 `animations` 数组中
2. 页面加载触发 `enter` 动画，通过 `async/await` 按顺序依次执行
3. 使用 animate.css 类名（`.animated` + 动画类型名），通过 CSS `@keyframes` 驱动
4. 支持 `infinite` 无限循环和 `animation-duration` 自定义时长

**追问：动画性能怎么保证？**

项目中的动画基于 CSS `@keyframes` + `transform`/`opacity` 实现，不涉及 JS 逐帧计算。CSS 动画由浏览器的合成器（Compositor）在 GPU 上处理，不占用主线程，不会阻塞交互操作（拖拽、输入等）。具体做法：
- 动画属性限定使用 `transform`（translate、scale、rotate）和 `opacity`——这些属性触发合成器，不触发重排重绘
- 通过 `animationend` 事件监听动画结束并移除 class，避免多余 DOM 计算
- 对于 `infinite` 循环动画，只在预览态启用以减少编辑态的性能消耗

**追问：为什么用 CSS 动画，不用 JS 动画（GSAP/requestAnimationFrame）？**

CSS 动画适合本项目的业务场景：动画类型固定（预置的 fadeIn、bounceIn 等 74 种），不需要动态计算动画路径或做复杂的帧级控制。CSS 动画由浏览器合成器在 GPU 上处理，不占用 JS 主线程，与编辑器拖拽操作互不干扰。如果用 GSAP 或 rAF 做 JS 动画，虽然控制粒度更细，但在这种"编辑器实时拖拽 + 动画同时播放"的场景下，JS 动画会占用主线程，可能影响拖拽流畅度。所以我们选了"够用且不影响交互"的 CSS 动画方案。

### 18.事件系统是怎么实现的？（对应简历：实现事件系统，支持交互事件绑定）

> **一句话记忆：** 事件配置存 `events` 字段 → 渲染时绑定监听 → 触发时执行动作

**事件存储：**
组件的 `events` 字段是一个 `Record<string, string>`，key 为事件类型，value 为事件参数：
```typescript
// 界面配置：选择"跳转事件" → 输入 URL
events: { "redirect": "https://example.com" }

// 界面配置：选择"alert 事件" → 输入提示内容
events: { "alert": "提交成功！" }
```

**当前支持的事件类型：**
- `redirect`：跳转到指定 URL（`window.open(url, '_blank')`）
- `alert`：弹窗提示（`window.alert(msg)`）

**联动系统（Linkage）：**
除标准事件外，还支持组件间联动（`src/custom-component/common/useOnEvent.ts`）：
- `v-click`：点击目标组件时触发样式变更
- `v-hover`：悬停目标组件时触发样式变更
- 通过 eventBus 发布订阅实现跨组件通信

**实现原理：**
1. `ComponentWrapper`（预览）/ `PreviewNodeRenderer`（iframe 预览）在渲染组件时绑定事件
2. 点击时读取 `config.events`，遍历各事件的 key 在 `events.ts` 的 handler 映射表中查找对应处理函数
3. 同时通过 eventBus 触发 `v-click` / `v-hover` 联动信号

### 19.页面数据是怎么导入导出的？（对应简历：JSON 导入导出，形成完整编辑闭环）

> **一句话记忆：** 导出 JSON = JSON.stringify → Blob；导出 HTML = 组件→内联HTML；导入 = 读取 JSON → 校验 → 注入 store

**一、导出 JSON**

点击工具栏"数据→导出"按钮，把当前页面数据打包成 JSON 文件下载：

1. 从 Pinia 取出 `componentData` 和 `canvasStyleData`
2. 包上版本号和时间戳，组成完整的导出结构
3. `JSON.stringify` 序列化 → `Blob` 生成文件 → 创建 `<a>` 标签触发下载

```typescript
const exportData = {
  version: '1.0.0',
  timestamp: Date.now(),
  canvasStyle: canvasStyleData.value,
  componentData: componentData.value,
}
const blob = new Blob([JSON.stringify(exportData, null, 2)], { type: 'application/json' })
```

**二、导出 HTML**

点击工具栏"数据→导出 HTML"按钮，将画布内容导出为自包含的独立 HTML 文件。核心原理（`src/utils/exportHtml.ts`）：

1. **组件→HTML 映射**：每种组件类型有对应的 HTML 渲染函数（VText → `<div>`、Picture → `<img>`、VTable → `<table>`、SVGStar/SVGTriangle → 内联 SVG 等）
2. **样式内联**：所有 CSS（`width`、`height`、`color`、`backgroundColor`、`transform: rotate()` 等）转为内联 `style` 属性
3. **动画嵌入**：内嵌 animate.css 子集关键帧（fadeIn、bounceIn、zoomIn 等 74 种），保留时长/延迟/循环配置
4. **事件绑定**：`redirect` 转为 `window.open()`，`alert` 转为 `alert()`
5. **嵌套处理**：`parentId` 关联的子组件递归渲染到父容器内部
6. **图表降级**：VChart（ECharts 动态图表）渲染为占位提示

导出的 `.html` 文件无需任何外部依赖，双击即可在浏览器中打开，支持交互（按钮跳转、弹窗）和动画。

**三、导入 JSON**

点击工具栏"数据→导入"按钮，上传 JSON 文件或粘贴文本：

1. 读取 JSON → `JSON.parse` 解析
2. 用 Zod 校验数据格式（字段是否完整、类型是否正确）
3. 校验通过后，重新生成组件 ID（防止重复），注入 Pinia store
4. 画布自动重绘

**四、自动保存**（详见 **Q9 数据持久化**）

自动保存机制统一由 `useAutoSave` composable 管理：脏标记 + 3 秒防抖写入 localStorage、60 秒兜底、`beforeunload` 关闭前保存。

### 20.多人协作是怎么实现的？

> **一句话记忆：** Yjs CRDT 数据同步 + WebSocket 通信 + IndexedDB 持久化 + 远程光标感知

项目已实现基于 **Yjs** CRDT 的多人实时协作，通过 URL 参数 `?collab=1` 启用（默认关闭，保持单机向后兼容）。

**1. 协同架构（3 层）**

| 层 | 技术 | 实现 |
|------|------|------|
| **数据同步层** | Yjs CRDT（`yjs` ^13.6.31） | `src/collab/yDoc.ts` 维护 Yjs 文档，Store 变更通过 `syncAllToYjs` / `syncStyleToYjs` / `syncComponentToYjs` / `syncRemoveToYjs` 镜像到 Yjs Doc |
| **通信层** | y-websocket（`y-websocket` ^3.0.0） | `src/collab/provider.ts` 封装 WebSocket Provider，连接协同服务器（`server/index.js`） |
| **持久化层** | y-indexeddb（`y-indexeddb` ^9.0.12） | 协同模式下数据持久化到 IndexedDB，替代 localStorage（`useAutoSave` 在协同启用时跳过 localStorage 写入） |

**2. 用户感知与远程光标**

- `src/collab/awareness.ts`：基于 Yjs Awareness 协议实现用户在线状态与光标位置广播
- `src/components/Editor/RemoteCursors.vue`：渲染其他协作者的远程光标
- `src/components/OnlineUsers.vue`：显示当前在线用户列表
- eventBus 新增协同事件：`remoteCursorMove`、`remoteCursorLeave`、`remoteSelection`、`collabUsersChange`

**3. 命令系统集成**

- `src/collab/commandContext.ts`：协同模式下注入命令上下文，命令操作通过 Yjs 同步
- `src/collab/undoOrigin.ts`：集成 Yjs UndoManager，协同模式下撤销/重做走 Yjs 原生机制
- 命令栈本身不参与协同同步（`src/composables/useCommandHistory.ts` 序列化到本地 IndexedDB，是个人操作历史）

**4. 初始化流程**

`src/main.ts` 中检测 `?collab=1`：
1. 调用 `initCommandContext()` 注入协同命令上下文
2. 调用 `initCollab()`（来自 `src/collab/useCollabStore.ts`）连接 WebSocket + 初始化 IndexedDB

**追问：为什么用 Yjs CRDT，不用 OT（操作转换）？**

Yjs 使用 CRDT（无冲突复制数据类型），每个客户端独立维护数据副本，自动合并无冲突。相比 OT 的优势：一是不需要中心服务器做操作转换，客户端可以离线编辑后自动合并；二是实现复杂度更低，Yjs 内置了合并算法；三是天然支持离线编辑 + 自动同步。OT 需要 Server 仲裁，离线场景处理复杂。Yjs 的 undo 管理器（UndoManager）也比自研命令模式更适合协同场景——它知道哪些操作是自己做的（可撤销），哪些是别人的（不可撤销）。

**追问：协同模式下命令模式还有用吗？**

有用。命令模式负责**本地的编辑交互**（拖拽、缩放、撤销重做），Yjs 负责**跨客户端的数据同步**。两者分工：命令模式产生数据变更 → Store 的 Yjs 镜像函数将变更同步到 Yjs Doc → Yjs 自动广播给其他客户端。协同模式下的撤销重做走 Yjs UndoManager（而不是 CommandManager 的双栈），因为协同场景下"撤销"只能撤销自己的操作，不能撤销别人的——Yjs UndoManager 天然支持这个语义。

**追问：单机模式和协同模式怎么切换？**

通过 URL 参数切换：不加 `?collab=1` 就是单机模式（localStorage 持久化，CommandManager 撤销重做）；加了就是协同模式（IndexedDB 持久化，Yjs UndoManager 撤销重做）。两种模式共用同一套 Store 和组件代码，差异仅在持久化层和撤销管理层，设计上做到了向后兼容。

---

## 高频追问速查表

| 追问 | 对应题号 | 核心回答 |
|------|---------|---------|
| 为什么不用树形结构？ | 1 | 扁平数组配合 `parentId`，查找/更新 O(n)，配合 Map 可 O(1) |
| 旋转后拖拽坐标怎么算？ | 2 | 三角函数 `globalX = centerX + localX·cos(θ) - localY·sin(θ)` |
| 命令模式和备忘录模式区别？ | 3 | 命令模式封装操作本身（反向执行），备忘录保存完整快照 |
| 为什么选 Pinia 不选 Vuex？ | 12 | 更轻量、无 mutations、原生 TS 支持、扁平模块结构 |
| 响应式失效怎么排查？ | 13 | 检查是否整体替换对象导致 Proxy 引用断裂，改用字段级赋值 |
| 组件库怎么扩展？ | 10 | 编写组件 → 调用 `registerComponent()` 注册 → 自动识别 |
| 拖入容器怎么判断？ | 10 | `findContainerAt()` 倒序遍历，坐标在容器边界内则设 `parentId` |
| 属性面板怎么适配不同组件？ | 10 | `propConfigs` 元数据 + `controlMap` 控件注册表，按 `type` 动态切换 |
| 导出的格式有哪些？ | 19 | JSON（数据交换）+ HTML（自包含文件，双击即开） |
| 容器组件怎么渲染子组件？ | 10 | NodeRenderer 递归查找 `parentId === node.id`，通过 slot 注入 |
| 拖拽时怎么保证流畅？ | 2, 5 | RAF 节流 mousemove + DOM 直写 transform（每帧归零）+ 视口裁剪 |
| localStorage 容量不够怎么办？ | 9 | 数据压缩（规划中）+ 写入失败时降级 IndexedDB（规划中） |
| iframe 预览怎么通信？ | 8 | `postMessage({ source: 'editor-preview', type: 'componentData', data })` |
| 组件嵌套怎么删子组件？ | 10 | DeleteComponentCommand 递归查 parentId 级联删除 |
| 快捷键怎么防止和浏览器冲突？ | 16 | `e.preventDefault()` 阻止默认行为（如 Ctrl+Z 浏览器前进） |
| 动画系统支持哪些类型？ | 17 | 进入/退出动画 + 强调动画，基于 animate.css 关键帧（内嵌 SCSS） |
| 事件系统支持哪些动作？ | 18 | redirect（跳转链接）+ alert（弹窗提示），联动支持 v-click/v-hover |
| 组合和拆分怎么实现？ | 10 | 组合：子组件从 `componentData` 搬入 Group 的 `propValue`；拆分：反向操作，还原绝对坐标 |
| HTML 导出支持哪些组件？ | 19 | VText/VButton/Picture/RectShape/CircleShape/LineShape/SVG/Table，VChart 降级为占位图 |
| 协同用什么技术？ | 20 | Yjs CRDT（yjs）+ y-websocket 通信 + y-indexeddb 持久化，`?collab=1` 启用 |
| 单机和协同怎么切换？ | 20 | URL 参数 `?collab=1`，单机走 localStorage + CommandManager，协同走 IndexedDB + Yjs UndoManager |
| 协同模式撤销重做走什么？ | 20 | Yjs UndoManager（非 CommandManager 双栈），只能撤销自己的操作 |
