# Vue3 低代码平台：项目流程详解

> 项目目录：`D:\Vue3_低代码平台`
> 目标：让没学过低代码编辑器的人也能看懂完整流程，并可用于前端面试复习。

---

## 1. 一句话理解项目

这是一个"可视化网页编辑器"，类似简化版的 Wix 或 Webflow，用户通过拖拽组件就能生成营销活动页面。

### 1.1 什么是营销页面？

营销页面是指用于商业推广和用户转化的专题页面：

| 类型 | 示例场景 |
| --- | --- |
| 活动页 | 双11大促、618购物节、周年庆典 |
| 落地页 | 新用户拉新、产品发布、限时优惠 |
| 专题页 | 品牌宣传、新品首发、节日营销 |

**营销页面特点**：
- 上线频繁
- 样式要求灵活
- 页面变化快
- 技术不希望每次都手写代码

**传统做法**：前端每次写一个新页面
**这个平台的做法**：运营拖拽组件 → 自动生成页面

### 1.2 用户在页面里做这些事

1. 从左侧拖一个组件（文本、按钮、图表等）到画布
2. 在画布中移动、缩放、旋转组件
3. 在右侧修改样式、配置动画、绑定事件
4. 用工具栏保存、预览、导入导出 JSON、撤销重做、截图

**本质**：它把"写代码搭页面"变成"拖拽搭页面"。

---

## 2. 系统架构

### 2.1 三栏式编辑器布局

```
┌─────────────┬─────────────────────────┬─────────────┐
│             │                         │             │
│  组件面板    │       画布编辑区         │  属性配置区  │
│             │                         │             │
│  - 文本     │    ┌───────────────┐    │  - 样式     │
│  - 图片     │    │               │    │  - 属性     │
│  - 按钮     │    │   可拖拽组件   │    │  - 事件     │
│  - 表格     │    │               │    │  - 动画     │
│  - 图表     │    └───────────────┘    │  - 联动     │
│  - 图形     │                         │             │
│             │                         │             │
└─────────────┴─────────────────────────┴─────────────┘
```

### 2.2 技术栈

| 技术 | 版本 | 作用 |
| --- | --- | --- |
| **Vue 3** | ^3.2.47 | 核心框架，使用 Composition API 开发，响应式渲染组件 |
| **TypeScript** | ^5.7.0 | 类型约束，通过接口定义组件属性结构，保障编译时类型安全 |
| **Pinia** | ^2.0.32 | 状态管理，管理画布数据、组件状态及命令历史 |
| **Vue Router** | ^4.1.6 | 路由管理，支持多页面编辑切换与鉴权守卫 |
| **Element Plus** | ^2.3.0 | UI 组件库，用于属性面板和侧边栏（全局 size: 'small'） |
| **Vite** | ^6.1.0 | 构建工具，极速的热更新体验 |
| **Zod** | ^4.3.6 | 运行时数据校验，校验导入的 JSON 数据格式（编译时 TS + 运行时 Zod 双重保障） |
| **ECharts** + **vue-echarts** | ^5.4.1 / ^6.5.4 | 数据可视化支持，VChart 组件按需引入图表类型 |
| **Ace Editor** | ^1.12.3（ace-builds） | JSON 编辑器 |
| **html-to-image** | ^1.9.0 | 预览截图 |
| **Animate.css（内嵌）** | - | 动画关键帧，内嵌于 `src/styles/animate.scss` 编译使用（非 npm 依赖），含 74 种预置动画 |
| **nanoid** | ^4.0.0 | 唯一 ID 生成 |
| **axios** | ^1.18.1 | HTTP 请求库，用于用户认证与服务端 API 交互 |
| **DOMPurify** | ^3.4.11 | XSS 防护，净化富文本内容与 HTML 导出输出 |
| **Yjs** | ^13.6.31 | 协同编辑 CRDT 框架（通过 `?collab=1` 启用） |
| **y-websocket** | ^3.0.0 | Yjs WebSocket 通信层 |
| **y-indexeddb** | ^9.0.12 | Yjs IndexedDB 持久化 |
| **Vitest** | ^3.1.0 | 单元测试框架 |

### 2.3 分工

1. `views/Home.vue`：编辑器主页面（左中右布局）；另含 `Login.vue` / `Register.vue` / `Dashboard.vue` / `PreviewPage.vue`
2. `components/Editor/*`：画布引擎（拖拽、选中、框选、标线、右键、远程光标）
3. `store/index.ts`：核心状态管理（组件数据、选中状态、画布配置 + Yjs 镜像）；`store/auth.ts` 用户认证
4. `custom-component/*`：低代码物料（组件 + 属性编辑面板 + 控件注册表）
5. `collab/*`：协同编辑（Yjs CRDT + WebSocket + 远程光标，通过 `?collab=1` 启用）
6. `utils/*`：工具函数（快捷键、动画执行、请求、样式计算、XSS 净化、HTML 导出）
7. `types/index.ts`：TypeScript 类型定义（组件数据、画布配置、命令 Payload 等接口）
8. `schemas/index.ts` + `utils/validation.ts`：Zod Schema 定义 + 运行时校验工具函数
9. `server/`：后端服务（`index.js` WebSocket 协同服务器 + `app.js` REST API 用户认证）

---

## 3. 核心设计思想

### 3.1 页面数据结构设计（最关键）

**核心思想**：页面不是 DOM，而是一棵由 JSON 描述的组件树。

> 项目中有两个层次的数据描述：
> - **TypeScript 接口**（`src/types/index.ts`）— 编译时约束，定义组件数据结构长什么样
> - **Zod Schema**（`src/schemas/index.ts`）— 运行时校验，导入数据时检查格式是否正确
>
> 两者是互补关系：TS 在开发阶段拦截类型错误，Zod 在运行时拦截外部非法数据。简历中说的"设计 JSON Schema"实际是指 **Zod**（一个 TS 原生的 Schema 校验库，可类比为 TypeScript 版的 JSON Schema）。

采用扁平数组 + `parentId` 描述组件树层级关系，通过 TypeScript 接口与 Zod 运行时校验保障类型安全。

```typescript
// 页面数据结构示例
{
  "id": "page_001",
  "title": "双11活动页",
  "components": [
    {
      "id": "text_001",
      "component": "VText",
      "label": "标题文本",
      "style": {
        "position": "absolute",
        "top": 100,
        "left": 200,
        "width": 300,
        "height": 50,
        "fontSize": 24,
        "color": "#333333"
      },
      "propValue": "限时优惠",
      "animations": [],
      "events": {},
      "linkage": { duration: 0, data: [] }
    },
    {
      "id": "image_001",
      "component": "Picture",
      "style": { "top": 200, "left": 100, "width": 400, "height": 300 },
      "propValue": { "url": "https://example.com/banner.jpg", "flip": { "horizontal": false, "vertical": false } }
    }
  ]
}
```

**渲染原理**：Vue 通过 `<component :is="componentMap[item.component]" v-bind="item" />` 动态渲染组件，实现数据驱动视图。

**这本质是**：编辑态是 JSON，运行态也是 JSON 驱动渲染。

### 3.2 命令模式与撤销重做

将用户操作抽象为命令对象，维护撤销栈和重做栈，支持操作合并和批量命令。

**核心架构**：

```
用户操作 → 创建 Command 对象 → CommandManager.execute()
                                    ├─ 执行 command.execute()
                                    ├─ 压入 undoStack
                                    └─ 清空 redoStack

撤销 → undoStack.pop() → command.undo() → 压入 redoStack
重做 → redoStack.pop() → command.redo() → 压入 undoStack
```

**CommandManager 实现**：

```typescript
export class CommandManager {
  private undoStack: Command[] = []
  private redoStack: Command[] = []
  private readonly config: CommandManagerConfig

  constructor(config: Partial<CommandManagerConfig> = {}) {
    this.config = {
      maxStackSize: config.maxStackSize ?? 50,    // 最大历史记录数
      mergeTimeWindow: config.mergeTimeWindow ?? 300, // 合并时间窗口(ms)
    }
  }

  execute(command: Command): void {
    const lastCommand = this.undoStack[this.undoStack.length - 1]

    // 命令合并：连续的同类操作合并为一条（如连续移动）
    if (this.shouldMerge(lastCommand, command)) {
      const mergedCommand = lastCommand!.merge(command)
      this.undoStack[this.undoStack.length - 1] = mergedCommand
      mergedCommand.execute()
      this.redoStack = []
      return
    }

    command.execute()
    this.undoStack.push(command)
    this.redoStack = []

    // 栈大小限制，防止内存溢出
    if (this.undoStack.length > this.config.maxStackSize) {
      this.undoStack.shift()
    }
  }

  private shouldMerge(lastCommand: Command | undefined, command: Command): boolean {
    if (!lastCommand || !lastCommand.mergeable || !command.mergeable) return false
    const timeDiff = Math.abs(command.timestamp - lastCommand.timestamp)
    if (timeDiff > this.config.mergeTimeWindow) return false
    return lastCommand.canMergeWith(command, this.config.mergeTimeWindow)
  }
}
```

**命令接口设计**：

```typescript
export interface Command {
  execute(): void          // 执行命令
  undo(): void             // 撤销命令
  redo(): void             // 重做（默认调用 execute）
  description: string      // 命令描述
  type: CommandType        // 命令类型
  timestamp: number        // 时间戳（用于合并判断）
  mergeable: boolean       // 是否可合并
  canMergeWith(other: Command, mergeTimeWindow: number): boolean
  merge(other: Command): Command
}
```

**命令合并示例**（MoveCommand）：

```typescript
// 快速连续拖动同一组件 → 合并为一条命令
canMergeWith(other: Command): boolean {
  if (other.type !== CommandType.MOVE_COMPONENT) return false
  return this.positionData.componentId === other.positionData.componentId
}

merge(other: Command): MoveCommand {
  // 保留第一次的 oldStyle，取最后一次的 newStyle
  return new MoveCommand(
    this.positionData.componentId,
    this.positionData.oldStyle,   // 起始位置
    other.positionData.newStyle   // 最终位置
  )
}
```

### 命令清单

| 命令 | execute | undo | 可合并 |
|------|---------|------|--------|
| MoveCommand | 移动位置 | 恢复原位置 | ✅ |
| ResizeCommand | 缩放尺寸 | 恢复原尺寸 | ✅ |
| RotateCommand | 旋转角度 | 恢复原角度 | ✅ |
| AddComponentCommand | 添加组件 | 删除组件 | ❌ |
| DeleteComponentCommand | 删除组件 | 恢复组件 | ❌ |
| LayerCommand | 调整图层 | 恢复原图层 | ❌ |
| ComposeCommand | 组合组件 | 拆分组件 | ❌ |
| DecomposeCommand | 拆分组件 | 组合组件 | ❌ |
| CutCommand | 剪切 | 恢复 | ❌ |
| PasteCommand | 粘贴 | 删除 | ❌ |
| BatchCommand | 批量操作 | 批量回退 | ❌ |
| ClearCanvasCommand | 清空画布 | 恢复全部 | ❌ |
| ImportDataCommand | 导入数据 | 恢复原数据 | ❌ |

**关键设计决策**：
1. **命令模式 vs 备忘录模式**：命令模式只记录变化部分（增量），内存占用更低；备忘录模式每次深拷贝全量数据，内存开销大
2. **命令合并**：Move/Resize/Rotate 在 300ms 内的连续操作自动合并，用户快速拖动 100 次只产生 1 条 undo 记录
3. **栈大小限制**：最多 50 条历史记录，超出后丢弃最早的命令

### 3.3 状态管理与双向联动

基于 Pinia 实现全局状态管理，属性面板与画布状态实时双向联动：

```
点击画布组件 → 更新 curComponent → 属性面板读取 store → 修改数据 → 自动驱动画布更新
```

**本质**：响应式数据共享。

### 3.4 拖拽系统实现

**核心思想**：把鼠标位移映射成组件坐标变化，通过 RAF 节流 + DOM 直写实现高性能拖拽。

```typescript
// Shape.vue 中的拖拽核心逻辑（简化版）
function handleMouseDownOnShape(e: MouseEvent): void {
  const pos = { ...props.defaultStyle }
  let startY = e.clientY, startX = e.clientX
  let startTop = pos.top, startLeft = pos.left

  // RAF 节流：每帧只更新一次 store
  const throttledMove = createRAFThrottle((curX: number, curY: number) => {
    const newTop = startTop + (curY - startY)
    const newLeft = startLeft + (curX - startX)
    store.setShapeStyle({ top: newTop, left: newLeft })
    // 重设基线，下一帧的 transform 只计算增量偏移
    startY = curY; startX = curX; startTop = newTop; startLeft = newLeft
    shapeRef.value.style.transform = ''  // 清除 transform
  })

  const move = (moveEvent: MouseEvent) => {
    // 直接 DOM 操作：微量 transform，每帧 RAF 提交后会归零
    shapeRef.value.style.transform = `translate(${deltaX}px, ${deltaY}px)`
    throttledMove(moveEvent.clientX, moveEvent.clientY)
  }

  const up = () => {
    // 鼠标释放 → 通过 CommandManager 创建 MoveCommand 入栈（可撤销）
    moveComponent(props.element.id, oldStyle, newStyle) // 来自 useCommandActions
  }
}
```

**需要解决的问题**：
- 画布缩放比例适配（`changeStyleWithScale`）
- 元素自动吸附对齐（`MarkLine` + `editorContext`）
- 多选拖拽（框选 + Group）
- 边界限制
- 性能优化（RAF 节流、DOM 直写、命令合并）

### 3.5 图层管理

每个组件有独立的 `zIndex` 字段（值越大越靠上），通过显式 zIndex 管理层级：
- 上移一层 / 下移一层
- 置顶 / 置底
- 锁定 / 解锁

**底层本质**：上/下移 = 交换相邻 zIndex 值；置顶 = zIndex 设为当前最大值 + 1。

```typescript
// 图层操作实现（基于 zIndex 交换）
upComponent(): void {
  const curZ = this.curComponent!.zIndex
  // 与 zIndex = current + 1 的组件交换值
  const above = this.componentData.find(c => c.zIndex === curZ + 1)
  if (above) {
    above.zIndex = curZ
    this.curComponent!.zIndex = curZ + 1
  }
}

topComponent(): void {
  // 置顶：直接设为最大值 + 1，无需与任何人交换
  const maxZ = this.componentData.reduce((max, c) => Math.max(max, c.zIndex), 0)
  this.curComponent!.zIndex = maxZ + 1
}
```

**优点**：zIndex 精准控制每一层的上下关系，不受数组顺序和 CSS 层叠上下文影响。图层列表按 zIndex 降序排列，最上层组件显示在最顶部。

### 3.6 自动对齐

通过几何计算实现：
- 判断组件中心点/边缘差值
- 差值小于阈值（如 5px）时显示辅助线
- 自动吸附对齐

**涉及**：位置信息计算、实时碰撞判断。

### 3.7 版本管理

支持页面版本管理功能：
- **保存版本**：保存当前页面快照，可添加版本名称和描述
- **恢复版本**：一键恢复到历史版本
- **删除版本**：清理不需要的历史版本
- **本地存储**：版本数据持久化到 localStorage

```typescript
// 版本管理实现
interface PageVersion {
  id: string
  name: string              // 版本名称
  description: string       // 版本描述
  snapshot: ComponentData[] // 页面快照
  createdAt: string         // 创建时间
  thumbnail?: string        // 缩略图（base64）
}

saveVersion(name: string, description: string): void {
  const version: PageVersion = {
    id: generateID(),
    name,
    description,
    snapshot: deepCopy(this.componentData),
    createdAt: new Date().toISOString(),
  }
  this.versions.push(version)
  this.saveVersionsToStorage()
}

restoreVersion(versionId: string): void {
  const version = this.versions.find(v => v.id === versionId)
  if (version) {
    this.componentData = deepCopy(version.snapshot)
    this.curComponent = null
    this.curComponentIndex = null
    this.recordSnapshot()
  }
}
```

---

## 4. 项目启动后发生了什么

### 4.1 入口初始化

`src/main.ts` 做了：

1. 创建 Vue app
2. 调用 `registerAllCommands()` 初始化命令注册表（显式注册所有命令类型）
3. 挂载 Element Plus（全局配置 `size: 'small'`）
4. 挂载 Router
5. 挂载 Pinia
6. 安装自定义组件注册器 `installCustomComponents`
7. 挂载全局样式（animate.scss、reset.css、global.scss、dark.scss）
8. 协同编辑初始化：检测 URL `?collab=1`，启用时调用 `initCommandContext()` + `initCollab()`（连接 Yjs WebSocket + IndexedDB），默认关闭保持单机向后兼容

### 4.2 路由

`src/router/index.ts` 注册了 6 条路由，使用 `createWebHistory()` 模式，全部懒加载：

| path | name | 组件 | meta |
| --- | --- | --- | --- |
| `/` | Home | `Home.vue` | — |
| `/preview` | Preview | `PreviewPage.vue` | — |
| `/login` | Login | `Login.vue` | `{ guest: true }` |
| `/register` | Register | `Register.vue` | `{ guest: true }` |
| `/dashboard` | Dashboard | `Dashboard.vue` | `{ requiresAuth: true }` |
| `/editor/:id` | Editor | `Home.vue` | `{ requiresAuth: true }` |

路由守卫 `beforeEach` 基于 `localStorage.getItem('token')` 鉴权：需登录且无 token → 跳 `/login`（带 redirect 回跳）；已登录访问 guest 页 → 跳 `/dashboard`。

### 4.3 App 壳层

`src/App.vue` 很薄，只做 `<router-view />`，说明核心逻辑都在 `Home.vue` 和编辑器组件里。

---

## 5. 编辑器页面结构（Home.vue）

主页面是三栏：

1. 左侧：组件库 + 图层面板
2. 中间：画布编辑区
3. 右侧：属性/动画/事件面板

顶部是 `Toolbar`，提供全局操作：

1. JSON 查看与编辑
2. 导入/导出
3. 撤销/重做
4. 预览/保存/清空/截图
5. 组合/拆分
6. 锁定/解锁
7. 画布尺寸和缩放
8. 深浅色切换
9. 版本管理

---

## 6. 核心状态设计（Pinia Store）

`src/store/index.ts` 是项目大脑，关键状态：

| 状态 | 类型 | 描述 |
| --- | --- | --- |
| `editMode` | `'edit' \| 'preview'` | 编辑模式 |
| `canvasStyleData` | `CanvasStyleData` | 画布宽高、缩放、背景、字体等 |
| `componentData` | `ComponentData[]` | 画布上的全部组件数据 |
| `curComponent` | `ComponentData \| null` | 当前选中的组件 |
| `curComponentIndex` | `number \| null` | 当前选中组件的索引 |
| `isClickComponent` | `boolean` | 是否点击了组件 |
| `editor` | `HTMLElement \| null` | 画布 DOM 引用 |
| `copyData` | `CopyData \| null` | 复制/剪切缓存 |
| `areaData` | `AreaData` | 框选区域及命中的组件 |
| `menuShow/menuTop/menuLeft` | `boolean/number` | 右键菜单状态 |
| `isDarkMode` | `boolean` | 主题状态 |
| `rightList` | `boolean` | 右侧面板显示状态 |
| `isInEditor` | `boolean` | 是否在编辑器内（快捷键拦截用） |
| `versions` | `PageVersion[]` | 版本历史列表 |
| `dataVersion` | `number` | 数据版本号，每次变更递增，用于自动保存脏标记 |

另外 `src/store/auth.ts` 是独立的认证 Store（`useAuthStore`，组合式 API），管理 `token`、`user` 状态，提供 `login`/`register`/`logout` 方法，从 localStorage 恢复登录态。

关键动作（actions）——命令操作和版本管理已抽取至 composable：

| 分类 | Actions |
| --- | --- |
| 组件增删改 | `addComponent/deleteComponent/updateComponentProps/ensureZIndex` |
| 图层控制 | `upComponent/downComponent/topComponent/bottomComponent`（基于 zIndex 交换） |
| 动画与事件 | `addAnimation/removeAnimation/alterAnimation/addEvent/removeEvent` |
| 编辑能力 | `copy/paste/lock/unlock`（cut 已抽取至 `cutWithCommand`） |
| 模式与菜单 | `setEditMode/setInEditorStatus/showContextMenu/hideContextMenu/toggleDarkMode/toggleRightList` |
| 数据标记 | `markDataDirty`（递增 dataVersion，触发自动保存） |
| Yjs 镜像 | `syncAllToYjs/syncStyleToYjs/syncComponentToYjs/syncRemoveToYjs`（协同模式下同步到 Yjs Doc） |

> **命令操作**（`executeCommand/undo/redo/canUndo/canRedo`）和**带命令的编辑**（`moveComponent/resizeComponent/rotateComponent/addComponentWithCommand/deleteComponentWithCommand/pasteWithCommand/cutWithCommand/composeWithCommand/decomposeWithCommand/clearCanvasWithCommand/importDataWithCommand/layerOperation`）已抽取至 `useCommandActions` composable。`layerOperation` 走 `LayerCommand`（基于数组位置 moveIndex），与 store 的 `upComponent` 等（基于 zIndex）是两套图层操作语义。
> **版本管理**（`saveVersion/restoreVersion/deleteVersion`）已抽取至 `useVersionManager` composable。

### Composable 架构

从 `Home.vue` 中抽取了多个 Composable 实现关注点分离：

| Composable | 文件 | 职责 |
| --- | --- | --- |
| `useAutoSave` | `composables/useAutoSave.ts` | 自动保存到 localStorage（脏标记 + 防抖） |
| `useCommandActions` | `composables/useCommandActions.ts` | 命令操作（撤销/重做/命令执行，模块级单例 CommandManager） |
| `useCommandHistory` | `composables/useCommandHistory.ts` | 命令历史持久化（跨会话，序列化 undo/redo 栈到 IndexedDB） |
| `useVersionManager` | `composables/useVersionManager.ts` | 版本管理（保存/恢复/删除版本，持久化到 localStorage） |
| `useDragDrop` | `composables/useDragDrop.ts` | 从组件列表拖拽到画布的逻辑 |
| `usePanelToggle` | `composables/usePanelToggle.ts` | 左右侧面板的显示/隐藏 |
| `useEditorContext` | `composables/useEditorContext.ts` | 编辑器内部通信（provide/inject 替代 eventBus） |

### 数据校验架构

使用 Zod 进行运行时数据校验，确保导入的 JSON 数据符合预期格式：

| 文件 | 职责 |
| --- | --- |
| `schemas/index.ts` | Zod Schema 定义（ComponentData、CanvasStyle、ExportData 等） |
| `utils/validation.ts` | 校验工具函数（validateComponentData、validateAuto 等） |

```typescript
// 智能校验：自动识别数据格式
export function validateAuto(data: unknown): ValidationResult<{
  componentData: ComponentData[]
  canvasStyle?: CanvasStyleData
}> {
  if (Array.isArray(data)) {
    // 旧格式：直接是组件数组
    const result = validateComponentData(data)
    // ...
  }
  if (data && typeof data === 'object' && 'componentData' in obj) {
    // 新格式：包含 canvasStyle 和 componentData
    const result = validateExportData(data)
    // ...
  }
}
```

---

## 7. 关键业务流程

### 7.1 拖拽添加组件流程

1. 左侧 `ComponentList` 开始拖拽，写入组件索引到 `dataTransfer`，并创建自定义拖拽预览 ghost 元素。
2. 在 `Home.vue` 的中间画布区域触发 `drop`（通过 `useDragDrop` composable）。
3. 根据索引从 `component-list.ts` 拿组件模板，`deepCopy` 生成新实例。
4. 计算落点（鼠标位置 - 画布偏移）。
5. **容器检测**：`findContainerAt()` 倒序遍历所有组件，检查落点是否在某个容器（`acceptChildren: true`）的边界内：
   - **拖入容器**：坐标转为相对容器，设置 `parentId = container.id`
   - **拖入空白区域**：坐标相对画布，`parentId = null`（根级组件）
6. 生成唯一 id（使用 nanoid）。
7. 根据当前画布缩放修正尺寸。
8. `store.addComponentWithCommand` → 创建 `AddComponentCommand` → 命令入栈。

**理解**：拖拽一次 = 模板实例化 + 容器检测 + 坐标定位 + 命令入栈（可撤销）。

### 7.2 选中/移动/缩放/旋转流程

由 `Editor/Shape.vue` 负责：

1. 点击组件：设置当前选中组件。
2. 拖动组件：`mousemove` 时通过 CSS `transform: translate()` 提供即时视觉反馈，同时 `createRAFThrottle` 每帧批量更新 store。
3. 八点缩放：按拖拽点位计算新宽高和位置（`calculateComponentPositionAndSize`）。
4. 旋转：按鼠标相对中心点角度变化计算 `rotate`（`Math.atan2`）。
5. 鼠标释放后创建 `MoveCommand/ResizeCommand/RotateCommand` 入栈。
6. 拖动时通过 `editorContext.startMove()` 通知标线吸附。

**性能优化**：
- **RAF 节流**：`createRAFThrottle` 将多次 `mousemove` 合并为每帧一次 store 更新
- **DOM 直写**：拖拽期间直接操作 `transform`，不经过 Vue 响应式系统
- **命令合并**：快速连续移动/缩放/旋转自动合并为一条 undo 记录（300ms 窗口）

这是低代码编辑器"手感"的核心代码。

### 7.3 框选与组合流程

由 `Editor/index.vue + ComposeCommand` 完成：

1. 在空白画布按下并拖动，生成框选区域。
2. 计算框中命中的组件（排除锁定项）。
3. 多选命中后写入 `areaData`。
4. 点击"组合"→ `store.composeWithCommand()` → 创建 `ComposeCommand` → 命令入栈。
5. `ComposeCommand.execute()` 收集子组件、创建 Group、计算相对坐标、删除原组件。
6. `ComposeCommand.undo()` 恢复原组件、删除 Group。

```typescript
// ComposeCommand 核心逻辑
execute(): void {
  const store = useStore()
  const componentsToGroup: ComponentData[] = []
  const originalIndices: number[] = []

  // 收集要组合的组件
  for (const id of this.componentIds) {
    const idx = store.componentData.findIndex(c => c.id === id)
    if (idx === -1) continue
    componentsToGroup.push(store.componentData[idx])
    originalIndices.push(idx)
  }

  // 计算 Group 的包围盒
  // ... 计算 top/left/width/height

  // 创建 Group 组件
  this.groupComponent = {
    id: generateID(),
    component: 'Group',
    propValue: componentsToGroup,
    style: { top, left, width, height, rotate: 0 },
    // ...
  }

  // 子组件转为相对坐标（groupStyle）
  createGroupStyle(this.groupComponent)

  // 删除原组件，添加 Group
  for (let i = store.componentData.length - 1; i >= 0; i--) {
    if (this.componentIds.includes(store.componentData[i].id)) {
      store.componentData.splice(i, 1)
    }
  }
  store.componentData.push(this.groupComponent)
}
```

拆分相反：`DecomposeCommand` 把 Group 子组件恢复绝对坐标，加回画布，删除 Group。

### 7.4 图层管理流程

`RealTimeComponentList.vue` 按"顶层到低层"显示组件。

支持：

1. 点击图层选中组件
2. 上移/下移
3. 删除

实际操作落在 store 的图层动作并记录快照。

### 7.5 右键菜单流程

`Editor/ContextMenu.vue` 提供：

1. 复制、粘贴、剪切、删除
2. 锁定、解锁
3. 置顶、置底、上移、下移

右键"粘贴"可按鼠标位置粘贴（`paste(true)`），提升编辑效率。

### 7.6 撤销/重做流程（命令模式）

核心思想：每次有效编辑创建一个 Command 对象，维护 undo/redo 双栈。

```
执行操作 → CommandManager.execute(command)
              ├─ command.execute()     // 执行命令
              ├─ undoStack.push(cmd)   // 压入撤销栈
              └─ redoStack = []        // 清空重做栈

撤销 → undoStack.pop() → cmd.undo() → redoStack.push(cmd)
重做 → redoStack.pop() → cmd.redo() → undoStack.push(cmd)
```

**命令合并**（关键优化）：

```typescript
// MoveCommand 在 300ms 内的连续移动自动合并
// 用户快速拖动组件 100 次，undo 栈中只有 1 条记录
shouldMerge(lastCommand, newCommand): boolean {
  if (!lastCommand.mergeable || !newCommand.mergeable) return false
  const timeDiff = Math.abs(newCommand.timestamp - lastCommand.timestamp)
  if (timeDiff > 300) return false
  return lastCommand.canMergeWith(newCommand)
}
```

**撤销后状态刷新**：

```typescript
// undo/redo 后需要重新解析 curComponent 引用
// 因为 componentData 数组已经变了，旧引用可能失效
refreshCurComponent(): void {
  if (this.curComponent) {
    const idx = this.componentData.findIndex(c => c.id === this.curComponent!.id)
    if (idx !== -1) {
      this.curComponent = this.componentData[idx]
      this.curComponentIndex = idx
    } else {
      this.curComponent = null
      this.curComponentIndex = null
    }
  }
}
```

这是标准编辑器设计，比快照模式更高效。

### 7.7 预览与截图流程

1. 点击预览 -> 弹出 `Preview.vue`。
2. `Preview.vue` 深拷贝当前组件数据，避免污染编辑态。
3. 每个组件通过 `ComponentWrapper` 挂载并执行动画/事件。
4. 若是截图模式，调用 `html-to-image` 生成 PNG 下载。

### 7.8 保存与恢复流程

1. 手动保存：`Toolbar.save` 把 `componentData` 和 `canvasStyleData` 写到 localStorage。
2. 自动保存：`useAutoSave` composable 监听 `dataVersion` 变化（脏标记），防抖 3 秒后自动保存，60 秒兜底保存。
3. 进入页面时 `Home.restore` 从 localStorage 恢复画布。
4. 页面关闭前 `beforeunload` 事件触发最终保存。

**性能优化**：自动保存使用 `dataVersion` 脏标记替代 `deep watch`，避免 drag 期间每帧触发 localStorage 写入。保存时检查版本号，数据未变化则跳过。

这保证了"刷新后设计不丢"。

### 7.9 JSON 导入导出与代码编辑

1. 导出：把 `componentData` 序列化成 JSON 文本并下载。
2. 导入：支持粘贴 JSON 或上传 JSON 文件。
3. Ace 编辑器：可直接编辑当前组件或画布配置 JSON，再提交回 store。

这相当于"可视化模式"和"代码模式"的双通道。

### 7.10 版本管理流程

1. 点击"保存版本" -> 弹出版本信息填写框
2. 填写版本名称和描述 -> 深拷贝当前 `componentData` 保存到 `versions` 数组
3. 版本数据持久化到 localStorage
4. 点击"恢复版本" -> `importDataWithCommand(deepCopy(version.snapshot))`
5. 恢复操作通过 `ImportDataCommand` 入栈，支持撤销

```typescript
// 版本管理实现
interface PageVersion {
  id: string
  name: string              // 版本名称
  description: string       // 版本描述
  snapshot: ComponentData[] // 页面快照
  createdAt: string         // 创建时间
  thumbnail?: string        // 缩略图（base64）
}

saveVersion(name: string, description: string): void {
  const version: PageVersion = {
    id: generateID(),
    name,
    description,
    snapshot: deepCopy(this.componentData),
    createdAt: new Date().toISOString(),
  }
  this.versions.push(version)
  this.saveVersionsToStorage()
}

restoreVersion(versionId: string): void {
  const version = this.versions.find(v => v.id === versionId)
  if (version) {
    // 通过命令系统恢复，支持撤销
    this.importDataWithCommand(deepCopy(version.snapshot))
  }
}
```

---

## 8. 低代码运行态能力（不仅是拖拽）

### 8.1 动画系统

1. 在右侧动画面板给组件添加动画序列。
2. `runAnimation` 按顺序执行动画并支持循环标记。
3. 预览时组件挂载自动执行动画。

动画配置：
```typescript
interface Animation {
  type: string         // 动画类型（如 fadeIn, slideIn 等）
  duration: number     // 持续时间（ms）
  delay: number        // 延迟时间（ms）
  iterationNum: number // 执行次数
  infinite: boolean    // 是否无限循环
  applyTo: 'enter' | 'leave'  // 应用时机
}
```

### 8.2 事件系统

目前提供事件（`src/utils/events.ts`）：

1. `redirect`：安全跳转 URL（验证协议，阻止 `javascript:`，使用 `window.open` + `noopener,noreferrer`）
2. `alert`：弹窗提示（`window.alert`）

在预览态点击组件会读取 `config.events` 并执行对应 handler。

事件总线使用泛型 `EventMap` 接口实现类型安全（`src/utils/eventBus.ts`）：

```typescript
export interface EventMap {
  runAnimation: []
  stopAnimation: []
  preview: [screenshot: boolean]
  save: []
  clearCanvas: []
  hideArea: []
  componentClick: []
  move: [isDown: boolean, isRight: boolean]
  unmove: []
  'v-click': [id: string]
  'v-hover': [id: string]
  // 协同编辑相关
  remoteCursorMove: [userId: number, x: number, y: number]
  remoteCursorLeave: [userId: number]
  remoteSelection: [userId: number, componentIds: string[]]
  collabUsersChange: []
}
```

编辑器内部通信已逐步迁移到 `provide/inject`（通过 `useEditorContext` composable），eventBus 保留用于 Store → Editor 的跨层级通信和协同事件。

### 8.3 组件联动系统

`common/useOnEvent.ts` 通过类型安全的事件总线监听：

1. `v-click`
2. `v-hover`

当某组件触发事件时，目标组件可按配置自动改样式（联动效果）。

```typescript
// 联动配置
interface LinkageConfig {
  duration: number
  data: LinkageItem[]
}

interface LinkageItem {
  id: string          // 目标组件 ID
  label: string       // 目标组件标签
  event: string       // 触发事件
  style: Array<{ key: string; value: string }>  // 样式变更
}
```

### 8.4 数据请求能力

`utils/request.ts` 支持组件请求：

1. GET/POST/PUT/DELETE
2. 一次请求或轮询请求（series）
3. 参数类型处理
4. 请求次数限制

```typescript
interface RequestConfig {
  method: 'GET' | 'POST' | 'PUT' | 'DELETE'
  data: Record<string, unknown>[]
  url: string
  series: boolean      // 是否定时发送请求
  time: number         // 定时更新时间(ms)
  paramType: '' | 'string' | 'object' | 'array'
  requestCount: number // 请求次数限制，0 为无限
}
```

说明项目不仅能"摆组件"，也有"数据驱动组件"的基础能力。

---

## 9. 快捷键体系（效率能力）

`utils/shortcutKey.ts` 实现编辑器常见快捷键（使用 `e.key` 替代已废弃的 `keyCode`）：

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl/Cmd + C` | 复制 |
| `Ctrl/Cmd + V` | 粘贴 |
| `Ctrl/Cmd + X` | 剪切 |
| `Ctrl/Cmd + D` | 删除组件 |
| `Ctrl/Cmd + Z` | 撤销 |
| `Ctrl/Cmd + Y` | 重做 |
| `Ctrl/Cmd + G` | 组合 |
| `Ctrl/Cmd + B` | 拆分 |
| `Ctrl/Cmd + L` | 锁定 |
| `Ctrl/Cmd + U` | 解锁 |
| `Ctrl/Cmd + S` | 保存 |
| `Ctrl/Cmd + P` | 预览 |
| `Ctrl/Cmd + E` | 清空 |
| `Delete` | 删除组件 |

并根据"是否在编辑器内、组件是否锁定"动态决定可执行动作。

---

## 10. 组件元数据模型（为什么可扩展）

### 10.1 组件注册表

所有组件通过 **组件注册表**（`src/custom-component/registry.ts`）统一管理，提供动态注册 API：

```typescript
registerComponent('VText', VTextComp, {
  type: 'VText',
  label: '文字',
  icon: 'wenben',
  acceptChildren: false,       // 是否容器
  internal: false,              // 是否在组件面板隐藏
  propConfigs: [                // 属性面板元数据（驱动 PropPanelRenderer）
    { key: 'propValue', label: '文字内容', type: 'textarea' },
  ],
})
```

注册表的 `getPublicComponents()` 驱动左侧组件列表、`getAttrComponent()` 驱动属性面板回退。

### 10.2 组件模板默认值

`custom-component/component-list.ts` 定义了每个组件的初始化模板，统一包含：

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| `component` | `string` | 组件名 |
| `label` | `string` | 显示标签 |
| `icon` | `string` | 图标 |
| `propValue` | `PropValue` | 业务数据 |
| `style` | `ComponentStyle` | 样式配置 |
| `request` | `RequestConfig` | 接口配置（可选） |
| `animations` | `Animation[]` | 动画配置 |
| `events` | `Record<string, string>` | 事件配置 |
| `linkage` | `LinkageConfig` | 联动配置 |
| `isLock` | `boolean` | 是否锁定 |
| `parentId` | `string \| null` | 父组件 ID（null 表示根级） |
| `slot` | `string` | 插槽名称 |

初始化时自动合并 `commonAttr + commonStyle`。

### 10.3 propConfigs 属性面板元数据

组件注册时的 `propConfigs` 决定了属性面板显示哪些编辑项：

```typescript
[
  { key: 'propValue',          label: '文字内容', type: 'textarea' },
  { key: 'style.color',        label: '文字颜色', type: 'color' },
  { key: 'style.fontSize',     label: '字号',     type: 'number', min: 12, max: 72 },
  { key: 'style.textAlign',    label: '对齐方式', type: 'select', options: [...] },
  { key: 'propValue.flip.horizontal', label: '水平翻转', type: 'switch' },
]
```

- `key` 支持嵌套路径（如 `style.color`、`propValue.flip.horizontal`）
- `type` 映射到 `controls/` 中的控件组件
- 无 propConfigs 的组件（VTable/VChart）自动回退到传统 Attr.vue

### 10.4 可用组件列表

| 组件 | 类型 | 描述 |
| --- | --- | --- |
| VText | 基础 | 文本组件，支持富文本编辑 |
| VButton | 基础 | 按钮组件，支持事件绑定 |
| Picture | 基础 | 图片组件，支持翻转 |
| RectShape | 图形 | 矩形组件（唯一 acceptChildren 容器） |
| CircleShape | 图形 | 圆形组件 |
| LineShape | 图形 | 直线组件 |
| SVGStar | SVG | 星形组件 |
| SVGTriangle | SVG | 三角形组件 |
| VTable | 高级 | 表格组件，支持数据绑定 |
| VChart | 高级 | ECharts 图表组件（vue-echarts 按需引入） |
| Group | 容器 | 组合组件（internal，不在面板显示） |

---

## 11. 新手视角：一次完整操作实例

以"做一个会跳转的按钮卡片"为例：

1. 拖入矩形、文本、按钮
2. 调整位置、尺寸、颜色
3. 给按钮添加 `redirect` 事件
4. 给卡片加进入动画
5. 保存版本（可选）
6. 保存到本地
7. 点预览验证交互
8. 导出 JSON 给别人导入

这就是低代码平台交付页面原型的完整链路。

---

## 12. 面试可讲的关键亮点（大厂风格）

| 亮点 | 描述 |
| --- | --- |
| 编辑器核心能力完整 | 拖拽、缩放、旋转、框选、组合、图层、右键菜单 |
| 命令模式撤销重做 | 双栈 + 命令合并（300ms 窗口），快速连续操作只产生 1 条 undo 记录 |
| 性能优化体系 | RAF 节流、DOM 直写 transform、视口裁剪、O(1) 索引查找、脏标记自动保存 |
| 组件嵌套 | parentId 递归渲染，容器组件（RectShape）自动接收子组件，级联删除 |
| 元数据驱动属性面板 | propConfigs + 控件注册表，新增组件只需写配置无需写 Attr.vue |
| 动态组件注册 | registerComponent API，运行时动态注入组件，无需改编辑器代码 |
| iframe 隔离预览 | 独立路由 + postMessage 通信，样式与编辑器完全隔离 |
| HTML 导出 | 所有组件转换为自包含 HTML，双击即可打开，支持样式/动画/事件 |
| Composable 架构 | useAutoSave / useDragDrop / usePanelToggle / useEditorContext，关注点分离 |
| provide/inject 通信 | 编辑器内部用 provide/inject 替代 eventBus，类型安全、作用域隔离 |
| Zod 运行时校验 | `src/schemas/index.ts` 定义完整数据结构，`validateAuto` 智能识别格式，编译时 TS + 运行时 Zod 双重保障 |
| 协同编辑 | 基于 Yjs CRDT 的多人实时协作，`?collab=1` 启用，支持远程光标、在线用户感知、Yjs UndoManager |
| 用户认证 | 登录/注册/仪表盘页面，基于 token 的路由守卫，REST API 后端服务 |
| 命令时间线 | undo 历史可视化（CommandTimeline 组件），命令栈跨会话持久化到 IndexedDB |
| XSS 防护 | DOMPurify 净化富文本，HTML 导出时对 URL/CSS 安全校验，redirect 阻止 javascript: 协议 |
| 可扩展协议 | 组件元数据统一 + 注册表动态管理，新增物料只需注册 |
| 版本管理 | 支持页面版本保存、恢复、删除，持久化到 localStorage |
| 双模式编辑 | 可视化 + JSON 编辑器 + HTML 导出 |
| 运行态能力 | 动画（74 种）、事件、联动、请求 |
| TypeScript 全量覆盖 | 完整类型定义 + Zod 运行时校验，编译时 + 运行时双重保障 |
| 单元测试 | Vitest + jsdom，commands/schemas/utils 共 13 个测试文件 |

---

## 13. 大厂面试常见追问（附高分答法）

### Q1：你这项目最难的是哪块？

答：最难的是编辑器交互一致性。移动、缩放、旋转、框选、组合都在改同一份状态，我通过命令模式统一收敛——每个操作封装为 Command 对象，通过 CommandManager 统一执行和管理历史。拖拽期间用 RAF 节流 + DOM 直写 transform 解耦视觉反馈和状态更新，保证流畅性。

### Q2：为什么用命令模式而不是快照模式？

答：快照模式每次操作都深拷贝全量 componentData，内存开销大。命令模式只记录变化部分（增量），内存占用更低。而且命令模式支持命令合并——快速连续拖动 100 次，undo 栈中只有 1 条记录（300ms 窗口内的同类操作自动合并）。快照模式无法做到这种细粒度优化。

### Q3：命令栈大小有限制吗？

答：有的，限制最多 50 条命令记录，防止内存溢出。当超过限制时，会移除最早的命令。Move/Resize/Rotate 命令支持合并，实际能回退的操作远超 50 次。

### Q4：怎么支持新增组件？

答：按 `component-list.ts` 元数据规范新增组件目录，提供 `Component.vue + Attr.vue`，注册后即可被拖拽、被编辑、被预览。

### Q5：项目离生产化还差什么？

答：
1. 协同编辑已实现基础版（Yjs CRDT + WebSocket），但缺少权限粒度控制（谁能编辑/只读）
2. 云端存储和版本管理（当前依赖 localStorage / IndexedDB，缺少服务端数据库持久化）
3. 更安全的表达式/脚本执行沙箱
4. 更完善的权限体系和审计日志
5. 更强的测试覆盖和性能监控
6. 组件市场和模板库

### Q6：性能瓶颈在哪里？怎么优化的？

答：主要在三个方面。第一，拖拽期间的高频更新——通过 RAF 节流 + DOM 直写 transform 解决，每帧只更新一次 store。第二，自动保存的 deep watch 开销——通过 dataVersion 脏标记解决，drag 期间不触发 deep watch。第三，大组件量下的渲染——通过视口裁剪（isInViewport）只渲染可视区域内的组件，O(1) 索引查找（componentIndexMap）避免 v-for 中的 O(n²) findIndex。

### Q7：这个项目难在哪？（标准回答思路）

答：
1. **页面数据结构设计**：如何用 JSON 描述复杂页面结构，支持嵌套组件、动态属性
2. **操作抽象与历史记录**：命令模式的设计，如何处理连续操作、批量操作
3. **拖拽与坐标系统**：鼠标坐标到画布坐标的转换，缩放比例适配
4. **数据与 UI 实时一致性**：响应式更新、性能优化、避免循环依赖
5. **可扩展组件机制**：组件注册、动态加载、属性配置面板自动生成

### Q8：版本管理是怎么实现的？

答：版本管理本质是快照的持久化存储。保存版本时，把当前 `componentData` 深拷贝存到 `versions` 数组，并持久化到 localStorage。恢复版本时，通过 `importDataWithCommand` 创建 `ImportDataCommand` 入栈，所以恢复版本本身也可以撤销。版本数据包含 id、name、description、snapshot、createdAt 字段。

### Q9：组合组件是怎么实现的？

答：组合时，把多个组件收集起来，创建一个 Group 组件，把收集的组件存到 `propValue` 字段。同时计算子组件相对于 Group 的相对坐标（`groupStyle`）。拆分时，把子组件恢复绝对坐标，加回画布，删除 Group。

### Q10：TypeScript 给项目带来了什么好处？

答：
1. **类型安全**：编译时发现类型错误，减少运行时 bug
2. **IDE 支持**：完整的智能提示和自动补全
3. **重构能力**：修改类型定义后，所有引用处自动标红
4. **文档化**：类型定义即文档，降低理解成本
5. **可维护性**：大型项目更易于维护和扩展

### Q11：命令合并是怎么实现的？

答：每个 Command 有 `mergeable`、`timestamp`、`canMergeWith()`、`merge()` 四个属性。CommandManager 在执行新命令时，检查栈顶命令是否可合并：条件是两者都 `mergeable`、时间差在 300ms 内、且 `canMergeWith()` 返回 true。合并时保留第一次的 `oldStyle` 和最后一次的 `newStyle`，效果是快速连续操作只产生一条 undo 记录。目前 Move、Resize、Rotate 三个命令支持合并。

### Q12：命令历史和版本管理有什么区别？

答：命令历史是自动的，用于撤销/重做，最多 50 条，存在内存中，每次操作自动入栈。版本管理是手动的，用户主动保存，存在 localStorage 中，可以跨会话保留。命令历史是细粒度的（每步操作），版本是粗粒度的（用户命名的快照）。恢复版本时会通过 ImportDataCommand 入栈，所以恢复版本也可以撤销。

### Q13：组件组合/拆分的难点是什么？

答：难点是坐标转换。组合时要把子组件的绝对坐标转换为相对于Group的相对坐标（groupStyle）。拆分时要反向转换，把相对坐标还原为绝对坐标。如果转换出错，拆分后组件位置会乱掉。

### Q14：为什么用 Pinia 不用 Vuex？

答：Pinia 更轻量，没有 mutations，只有 state、getters、actions。原生支持 TypeScript，类型推导更好。支持组合式 API 风格，代码更简洁。扁平化模块结构，没有嵌套模块。

### Q15：拖拽期间性能怎么优化的？

答：三层优化。第一层：`createRAFThrottle` 将多次 mousemove 合并为每帧一次 store 更新。第二层：拖拽期间直接操作 DOM 的 `transform` 属性，不经过 Vue 响应式系统，实现 60fps 即时反馈。第三层：RAF 回调中重设基线并清除 transform，让 store 的 `top/left` 反映真实位置，transform 只承担增量偏移。另外自动保存用 `dataVersion` 脏标记替代 deep watch，避免 drag 期间每帧触发 localStorage 写入。

### Q16：Composable 架构是怎么设计的？

答：从 Home.vue 中抽取了 4 个 Composable 实现关注点分离。`useAutoSave` 负责自动保存（脏标记 + 防抖），`useDragDrop` 负责拖拽放置逻辑，`usePanelToggle` 负责面板切换状态，`useEditorContext` 用 provide/inject 替代 eventBus 实现编辑器内部通信。好处是 Home.vue 从 500 行降到 ~150 行，每个 Composable 职责单一、可独立测试。

### Q17：provide/inject 替代 eventBus 有什么好处？

答：三个好处。第一，类型安全——`EditorContext` 接口定义了所有方法签名，编译时检查。第二，作用域隔离——只在 Editor 子组件树内可用，不会污染全局。第三，无内存泄漏风险——eventBus 需要手动 on/off，provide/inject 跟随组件生命周期自动清理。但 Store → Editor 的通信（如 `hideArea`）仍保留 eventBus，因为 Store 没有组件上下文。

### Q18：Zod 在项目中怎么用的？

答：Zod 是一个 TypeScript 原生的 Schema 校验库，可以看作是"TS 版的 JSON Schema"。我在项目中用它做导入 JSON 数据的运行时校验——`src/schemas/index.ts` 定义了完整的 Zod Schema（ComponentData、CanvasStyle、ExportData），`src/utils/validation.ts` 封装了校验函数。导入时调用 `validateAuto()` 自动识别数据格式（旧/新两种格式），校验失败返回具体错误路径和消息。

面试中如果说"用 JSON Schema 描述页面结构"，面试官可能会追问具体实现——这时可以补充说明实际用的是 **Zod**，它和 JSON Schema 的关系就像 **TypeScript 和 JavaScript**：同一种思想，但 TS/Zod 更简洁且有类型推导。这是 TypeScript 类型安全的运行时补充——TS 只在编译时检查，Zod 在运行时拦截非法数据。

---

## 14. 核心能力总结

| 能力 | 描述 |
| --- | --- |
| 数据抽象 | 把页面抽象为 JSON 数据结构（TypeScript 接口编译时约束 + Zod Schema 运行时校验） |
| 操作抽象 | 把用户操作抽象为命令对象（Command Pattern + 命令合并） |
| 数据驱动 | 用响应式数据驱动可视化系统（Pinia + Vue 3 Reactivity） |
| 编辑体验 | 所见即所得编辑体验（RAF 节流 + DOM 直写 transform） |
| 架构设计 | Composable 关注点分离 + provide/inject 类型安全通信 |
| 数据校验 | 编译时 TypeScript + 运行时 Zod 双重保障 |

**项目本质**：在 Vue 上搭建了一个"浏览器里的可视化网页生成器"，核心在于用 JSON 描述页面结构，用命令模式管理编辑行为，用响应式系统保证实时联动，用 Composable 实现架构分层。

---

## 15. 30秒 / 60秒 / 120秒口述模板

### 30秒

我做了一个 Vue3 + TypeScript 低代码可视化编辑器，支持拖拽搭建页面、属性编辑、动画事件配置、版本管理、预览和 JSON/HTML 导入导出。核心亮点包括：扁平数组 + parentId 的组件嵌套、命令模式撤销重做（支持操作合并）、registerComponent 动态注册 API、元数据驱动的属性面板、iframe 隔离预览、RAF + DOM 直写的拖拽性能优化，以及基于 Yjs CRDT 的协同编辑（`?collab=1` 启用）。

### 60秒

这个项目是一个前端低代码编辑器，页面是左中右三栏：左侧组件库和图层，中间画布，右侧属性动画事件。拖拽组件到画布后可以移动、缩放、旋转、组合、锁定，支持拖入容器组件（RectShape）自动设置 parentId 实现嵌套。所有状态集中在 Pinia，编辑操作封装为 Command 对象，通过 CommandManager 管理撤销重做双栈，支持命令合并——快速连续拖动只产生一条 undo 记录。组件通过 registerComponent 动态注册，属性面板由 propConfigs 元数据驱动自动渲染。拖拽性能通过 RAF 节流 + DOM 直写 transform 优化。代码架构上用 Composable 拆分关注点，用 provide/inject 替代 eventBus 实现类型安全的编辑器内部通信。预览采用 iframe + postMessage 隔离渲染，支持导出为自包含 HTML 文件。导入数据用 Zod 做运行时校验。工具栏提供预览、截图、保存、版本管理、JSON/HTML 导入导出和 Ace 代码编辑。

### 120秒

我把它当一个"可扩展编辑器内核"来做。入口层负责框架和插件注册，Home 层负责编辑器布局编排，Editor 层处理画布级交互，Store 层维护单一状态源。核心难点是多交互统一：拖拽、缩放、旋转、框选组合、组件嵌套都要落在同一份组件数据上且可回退。我通过命令模式解决——每个操作封装为 Command 对象，CommandManager 维护双栈，支持命令合并（300ms 窗口内同类操作自动合并）。性能上，拖拽期间用 RAF 节流 + DOM 直写 transform 解耦视觉反馈和状态更新，自动保存用 dataVersion 脏标记替代 deep watch。架构上，从 Home.vue 抽取 4 个 Composable（useAutoSave、useDragDrop、usePanelToggle、useEditorContext），编辑器内部通信用 provide/inject 替代 eventBus。组件模型设计了注册表系统，所有组件通过 registerComponent API 动态注入，附带 propConfigs 元数据驱动属性面板自动渲染，新增组件不需要改编辑器主流程。画布支持 parentId 递归嵌套——容器组件（RectShape）暴露 slot，NodeRenderer 递归渲染子组件。预览采用独立路由 `/preview` + iframe + postMessage，样式与编辑器完全隔离。导出功能支持自包含 HTML，所有组件转为内联样式 + 动画关键帧 + 事件绑定，双击即可在浏览器打开。导入数据用 Zod Schema 做运行时校验，和 TypeScript 编译时检查形成双重保障。协同编辑基于 Yjs CRDT 实现，通过 `?collab=1` 启用，支持远程光标、在线用户感知和 Yjs UndoManager，单机与协同模式共用一套 Store 和组件代码。

---

## 16. 复习优先级建议

1. 先背第 3 节（核心设计思想）和第 7 节（关键业务流程）
2. 再背第 12-13 节（亮点和追问）
3. 最后背第 15 节（口述模板）

只要把"拖拽到画布 -> Command 入栈 -> 状态更新 -> 预览运行"的主链路讲清楚，面试官会认为你具备编辑器类复杂前端项目经验。

---

## 17. 项目文件速查表

| 文件路径 | 作用 |
| --- | --- |
| `src/main.ts` | 入口文件，挂载插件 + 命令注册 + 协同初始化 |
| `src/App.vue` | 根组件 |
| `src/views/Home.vue` | 编辑器主页面 |
| `src/views/PreviewPage.vue` | iframe 预览独立页面（/preview 路由） |
| `src/views/Login.vue` | 登录页（/login，guest 守卫） |
| `src/views/Register.vue` | 注册页（/register，guest 守卫） |
| `src/views/Dashboard.vue` | 仪表盘页（/dashboard，requiresAuth） |
| `src/store/index.ts` | 主 Pinia Store（画布数据 + Yjs 镜像） |
| `src/store/auth.ts` | 用户认证 Store（token/user/login/register/logout） |
| `src/types/index.ts` | TypeScript 类型定义（无 enum，全用联合类型） |
| `src/schemas/index.ts` | Zod Schema 定义（运行时校验，递归 ComponentDataSchema） |
| `src/router/index.ts` | 路由配置（6 条路由 + beforeEach 鉴权守卫） |
| **协同编辑** | |
| `src/collab/index.ts` | 协同模块入口 |
| `src/collab/yDoc.ts` | Yjs 文档与数据镜像 |
| `src/collab/provider.ts` | WebSocket Provider 封装 |
| `src/collab/awareness.ts` | 用户在线状态与光标感知 |
| `src/collab/commandContext.ts` | 协同模式命令上下文注入 |
| `src/collab/undoOrigin.ts` | Yjs UndoManager 集成 |
| `src/collab/useCollabStore.ts` | 协同初始化 Hook（initCollab） |
| **Composables** | |
| `src/composables/useAutoSave.ts` | 自动保存（脏标记 + 防抖） |
| `src/composables/useCommandActions.ts` | 命令操作（撤销/重做/命令执行，模块级单例 CommandManager） |
| `src/composables/useCommandHistory.ts` | 命令历史持久化（跨会话，序列化 undo/redo 栈到 IndexedDB） |
| `src/composables/useVersionManager.ts` | 版本管理（保存/恢复/删除版本） |
| `src/composables/useDragDrop.ts` | 拖拽放置逻辑 |
| `src/composables/usePanelToggle.ts` | 面板切换状态 |
| `src/composables/useEditorContext.ts` | 编辑器上下文（provide/inject） |
| **Editor 组件** | |
| `src/components/Editor/index.vue` | 画布编辑器主入口 |
| `src/components/Editor/Shape.vue` | 组件包装器（拖拽、缩放、旋转） |
| `src/components/Editor/MarkLine.vue` | 对齐辅助线（通过 editorContext 监听移动） |
| `src/components/Editor/Grid.vue` | SVG 网格背景 |
| `src/components/Editor/Area.vue` | 框选区域 |
| `src/components/Editor/ContextMenu.vue` | 右键菜单 |
| `src/components/Editor/Preview.vue` | 预览模式 |
| `src/components/Editor/AceEditor.vue` | JSON 代码编辑器 |
| `src/components/Editor/ComponentWrapper.vue` | 预览态组件包装器 |
| `src/components/Editor/NodeRenderer.vue` | 递归组件渲染器（parentId 嵌套） |
| `src/components/Editor/PreviewNodeRenderer.vue` | 预览模式递归渲染器 |
| `src/components/Editor/RemoteCursors.vue` | 协同远程光标渲染 |
| `src/components/Toolbar.vue` | 顶部工具栏 |
| `src/components/ComponentList.vue` | 左侧组件列表 |
| `src/components/RealTimeComponentList.vue` | 图层列表 |
| `src/components/CanvasAttr.vue` | 画布属性配置 |
| `src/components/AnimationList.vue` | 动画列表 |
| `src/components/AnimationSettingModal.vue` | 动画设置弹窗 |
| `src/components/EventList.vue` | 事件列表 |
| `src/components/VersionHistory.vue` | 版本历史 |
| `src/components/CommandTimeline.vue` | 命令时间线（undo 历史可视化） |
| `src/components/OnlineUsers.vue` | 协同在线用户列表 |
| `src/components/Modal.vue` | 通用弹窗 |
| **命令系统** | |
| `src/commands/CommandManager.ts` | 命令管理器（双栈 + 合并 + 跨会话序列化） |
| `src/commands/BaseCommand.ts` | 命令基类（含 setCommandContext 上下文注入） |
| `src/commands/registry.ts` | 命令注册表（register + deserialize + deserializeStack） |
| `src/commands/MoveCommand.ts` | 移动命令（可合并） |
| `src/commands/ResizeCommand.ts` | 缩放命令（可合并） |
| `src/commands/RotateCommand.ts` | 旋转命令（可合并） |
| `src/commands/LayerCommand.ts` | 图层命令（LAYER_UP/DOWN/TOP/BOTTOM） |
| `src/commands/ComposeCommand.ts` | 组合命令 |
| `src/commands/DecomposeCommand.ts` | 拆分命令 |
| `src/commands/PasteCommand.ts` | 粘贴命令 |
| `src/commands/CutCommand.ts` | 剪切命令 |
| `src/commands/ClearCanvasCommand.ts` | 清空画布命令 |
| `src/commands/ImportDataCommand.ts` | 导入数据命令 |
| `src/commands/BatchCommand.ts` | 批量命令 |
| `src/commands/setup.ts` | 命令注册入口（registerAllCommands） |
| `src/commands/index.ts` | 命令模块导出 |
| `src/commands/types.ts` | 命令类型定义（CommandType 枚举 + Command 接口） |
| **组件物料** | |
| `src/custom-component/registry.ts` | 组件注册表（registerComponent API + propConfigs 类型） |
| `src/custom-component/component-list.ts` | 组件模板列表 |
| `src/custom-component/index.ts` | 组件注册入口（通过注册表注册） |
| `src/custom-component/PropPanelRenderer.vue` | 元数据驱动属性面板渲染器 |
| `src/custom-component/controls/index.ts` | 控件组件注册表（controlMap） |
| `src/custom-component/controls/InputControl.vue` | 输入框控件 |
| `src/custom-component/controls/TextareaControl.vue` | 多行文本控件 |
| `src/custom-component/controls/NumberControl.vue` | 数字控件（min/max/step） |
| `src/custom-component/controls/ColorControl.vue` | 颜色选择器控件 |
| `src/custom-component/controls/SelectControl.vue` | 下拉框控件 |
| `src/custom-component/controls/SwitchControl.vue` | 开关控件 |
| **工具函数** | |
| `src/utils/exportHtml.ts` | HTML 导出引擎（组件→内联 HTML + 动画 + 事件） |
| `src/utils/shortcutKey.ts` | 快捷键处理（使用 e.key，非 keyCode） |
| `src/utils/utils.ts` | 通用工具函数（deepCopy、swap、$、isPreventDrop） |
| `src/utils/style.ts` | 样式计算 |
| `src/utils/translate.ts` | 坐标变换（旋转矩阵、缩放比例） |
| `src/utils/calculateComponentPositionAndSize.ts` | 八点缩放计算（corner/edge 通用逻辑） |
| `src/utils/changeComponentsSizeWithScale.ts` | 画布缩放时调整组件尺寸 |
| `src/utils/performance.ts` | 性能工具（RAF 节流、视口裁剪、拖拽状态） |
| `src/utils/eventBus.ts` | 类型安全事件总线（EventMap 泛型，含协同事件） |
| `src/utils/validation.ts` | Zod 校验工具函数 |
| `src/utils/request.ts` | 数据请求（XMLHttpRequest + 定时轮询） |
| `src/utils/api.ts` | 服务端 API 交互（axios + 认证） |
| `src/utils/attr.ts` | 属性面板配置数据 |
| `src/utils/sanitize.ts` | XSS 净化（escapeHtml、isValidImageUrl、isValidCssColor） |
| `src/utils/runAnimation.ts` | 动画执行 |
| `src/utils/animationClassData.ts` | 动画分类数据（74 种：进入 33 + 强调 10 + 退出 31） |
| `src/utils/events.ts` | 事件处理（redirect、alert） |
| `src/utils/generateID.ts` | 唯一 ID 生成（nanoid） |
| `src/utils/decomposeComponent.ts` | 组件拆分 |
| **后端服务** | |
| `server/index.js` | WebSocket 协同服务器 |
| `server/app.js` | REST API 服务（用户认证） |
| **测试** | |
| `src/commands/__tests__/*.test.ts` | 命令系统测试（4 个文件） |
| `src/schemas/__tests__/schemas.test.ts` | Schema 校验测试 |
| `src/utils/__tests__/*.test.ts` | 工具函数测试（8 个文件） |
