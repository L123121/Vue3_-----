# Vue3 低代码平台：项目流程详解

> 本文根据当前仓库真实代码重新整理，重点用于项目讲解和面试复习。核心源码在 `src/`，后端与协同服务在 `server/`。

---

## 1. 项目一句话

这是一个基于 **Vue 3 + TypeScript + Pinia + Vite** 的可视化低代码页面搭建平台。用户可以拖拽文本、按钮、图片、图形、表格、图表等组件到画布，配置样式、动画、事件、联动和数据请求，并支持撤销重做、JSON 导入导出、HTML 导出、截图、版本快照、登录后的页面管理，以及基于 **Yjs + WebSocket + IndexedDB** 的协同编辑。

面试表达：

> 这个项目的核心不是拖拽本身，而是用一份可序列化的 `componentData + canvasStyleData` 描述页面，再围绕这份数据实现编辑态渲染、运行态预览、命令历史、组件扩展、保存恢复和多人协同。

---

## 2. 实际技术栈

| 方向 | 技术/文件 | 说明 |
| --- | --- | --- |
| 前端框架 | `vue@3`、`typescript`、`vite` | 入口 `src/main.ts` |
| 状态管理 | `pinia` | `src/store/index.ts` 编辑器状态，`src/store/auth.ts` 登录状态 |
| 路由 | `vue-router@4` | `src/router/index.ts`，使用 `createWebHistory` |
| UI | `element-plus` | 工具栏、弹窗、抽屉、表单等 |
| 图表 | `echarts`、`vue-echarts` | `VChart` 组件 |
| 代码编辑 | `ace-builds` | `AceEditor.vue` JSON 编辑 |
| 截图 | `html-to-image` | `Preview.vue` 中 `toPng` |
| 校验 | `zod` | `src/schemas/index.ts`、`src/utils/validation.ts` |
| 安全 | `dompurify` | `VText` 和 HTML 导出防 XSS |
| 协同 | `yjs`、`y-websocket`、`y-indexeddb`、`y-protocols` | `src/collab/*` |
| 后端 | `express`、`mongoose`、`bcrypt`、`jsonwebtoken`、`ws` | `server/app.js`、`server/yjs-ws.js` |

常用脚本：

```bash
npm run dev          # 前端开发
npm run build        # 前端构建
npm run type-check   # vue-tsc 类型检查
npm run lint:check   # ESLint 检查
npm run test:run     # Vitest 测试
npm run server       # server/index.js，只启动独立 Yjs WS，默认 1234
npm run api          # server/app.js，Express API + Yjs WS
```

`server/package.json` 中 `npm start` 对应 `node app.js`。完整后端能力（登录、页面 CRUD、分享、协同）应使用 `npm run api` 或进入 `server` 后 `npm start`。

---

## 3. 整体架构

### 3.1 编辑器三栏布局

主页面是 `src/views/Home.vue`：

```text
Toolbar：JSON/导入导出/撤销重做/预览/保存/协同
├─ 左侧：ComponentList + RealTimeComponentList
├─ 中间：Editor 画布 + NodeRenderer + Shape + MarkLine + ContextMenu
└─ 右侧：PropPanelRenderer / CanvasAttr / AnimationList / EventList / CommandTimeline
```

真实组件分工：

- `Toolbar.vue`：顶部工具栏，负责预览、截图、导入导出、保存、版本、协同开关。
- `ComponentList.vue`：左侧物料列表。
- `RealTimeComponentList.vue`：图层列表。
- `Editor/index.vue`：画布容器，负责框选、右键菜单、递归渲染入口、远端光标。
- `NodeRenderer.vue`：编辑态递归渲染，按 `parentId` 找子组件。
- `Shape.vue`：选中框、拖动、缩放、旋转、锁定图标。
- `PropPanelRenderer.vue`：元数据驱动属性面板，无法元数据渲染时回退 `Attr.vue`。

### 3.2 分层理解

```text
用户操作层：拖拽、点击、快捷键、右键、表单输入
编辑器组件层：Home / Toolbar / Editor / Shape / NodeRenderer / 属性面板
业务封装层：useDragDrop / useCommandActions / useAutoSave / useVersionManager
状态命令层：Pinia store / CommandManager / 各 Command
协同同步层：Yjs Doc / WebsocketProvider / IndexeddbPersistence / Awareness
运行态层：Preview / PreviewNodeRenderer / exportHtml
后端服务层：Express API / MongoDB / JWT / Yjs WebSocket
```

---

## 4. 启动流程

### 4.1 `src/main.ts`

实际启动顺序：

1. 引入 Vue、Element Plus、Router、Pinia、自定义组件安装器。
2. 引入动画、reset、全局样式、暗色主题样式。
3. 调用 `registerAllCommands()`，显式加载所有命令模块，触发命令注册。
4. 创建 app，安装 Element Plus、router、pinia、自定义组件。
5. 挂载到 `#app`。
6. 如果 URL 带 `?collab=1`，调用 `initCommandContext()` 和 `initCollab()` 开启协同。

### 4.2 路由

`src/router/index.ts` 当前路由：

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| `/` | `Home.vue` | 默认本地编辑器 |
| `/preview` | `PreviewPage.vue` | iframe 预览页 |
| `/login` | `Login.vue` | 登录页 |
| `/register` | `Register.vue` | 注册页 |
| `/dashboard` | `Dashboard.vue` | 页面管理，需登录 |
| `/editor/:id` | `Home.vue` | 编辑服务器页面，需登录 |

路由守卫用 `localStorage.token` 判断登录状态。未登录访问需认证页面会跳到 `/login?redirect=...`。

### 4.3 `Home.vue` 初始化

`Home.vue` 启动后会：

- 调用 `useAutoSave()` 监听脏标记自动保存。
- 协同开启时调用 `useCommandHistory()` 持久化命令历史。
- 调用 `useDragDrop()` 提供拖拽落点处理。
- 调用 `usePanelToggle()` 控制左右面板。
- 如果是 `/editor/:id`，通过 `pagesApi.get(id)` 从服务器加载页面。
- 否则从 `localStorage.canvasData/canvasStyle` 恢复本地页面。
- 注册全局快捷键。
- 恢复暗色模式。

---

## 5. 核心数据模型

### 5.1 为什么页面是一份 JSON

项目不保存 DOM，而是保存组件描述数据。核心类型在 `src/types/index.ts`：

```ts
interface ComponentData {
  id: string
  component: string
  label: string
  icon: string
  propValue: PropValue
  style: ComponentStyle
  parentId: string | null
  slot: string
  zIndex: number
  request?: RequestConfig
  animations: Animation[]
  events: Record<string, string>
  groupStyle: Record<string, unknown>
  isLock: boolean
  collapseName: string
  linkage: LinkageConfig
}
```

字段理解：

- `component`：决定渲染哪个低代码组件，如 `VText`、`Picture`、`RectShape`。
- `propValue`：组件业务内容，如文本、图片 URL、表格数据、图表 option。
- `style`：位置、尺寸、颜色、边框、字体、旋转等样式。
- `parentId`：表示嵌套关系，当前公开容器是 `RectShape`。
- `animations/events/linkage/request`：运行态能力。
- `isLock`：控制是否允许编辑。
- `zIndex`：层级字段，但当前图层命令主要移动数组索引。

面试表达：

> `componentData` 是页面 DSL。编辑器、预览、保存、导入导出、HTML 导出和协同都围绕这份 DSL 工作，所以页面天然可序列化、可校验、可回放、可协同。

### 5.2 画布配置

`canvasStyleData` 默认值：

```ts
{
  width: 1200,
  height: 740,
  scale: 100,
  color: '#000',
  opacity: 1,
  backgroundColor: '#fff',
  fontSize: 14,
}
```

它用于编辑器画布、预览页、HTML 导出、本地保存、服务器保存和 Yjs 协同中的 `yCanvas`。

### 5.3 Pinia 主状态

`src/store/index.ts` 维护：

| 状态 | 用途 |
| --- | --- |
| `editMode` | `edit` / `preview` |
| `componentData` | 页面组件数组 |
| `curComponent/curComponentIndex` | 当前选中组件 |
| `areaData` | 框选区域和被框选组件 |
| `copyData` | 复制/剪切数据 |
| `menuTop/menuLeft/menuShow` | 右键菜单 |
| `versions` | 页面版本快照 |
| `dataVersion` | 脏标记，用于自动保存和命令历史持久化 |

`dataVersion` 是性能优化点：每次数据变更递增，`useAutoSave` 只浅监听这个版本号，避免 deep watch 高频触发。

---

## 6. 组件注册与物料扩展

### 6.1 当前物料

物料模板来自 `src/custom-component/component-list.ts`：

| component | label | 说明 |
| --- | --- | --- |
| `VText` | 文字 | 双击编辑，内容经 DOMPurify 净化 |
| `VButton` | 按钮 | 配置按钮文案和样式 |
| `Picture` | 图片 | URL 和水平/垂直翻转 |
| `RectShape` | 矩形 | 当前公开容器，`acceptChildren: true` |
| `LineShape` | 直线 | 只有左右缩放点 |
| `CircleShape` | 圆形 | 图形组件 |
| `SVGStar` | 星形 | SVG 组件 |
| `SVGTriangle` | 三角形 | SVG 组件 |
| `VTable` | 表格 | 支持静态数据和请求更新数据 |
| `VChart` | 图表 | ECharts 图表 |
| `Group` | 组合 | 内部组件，不显示在物料面板 |

### 6.2 注册表

`src/custom-component/registry.ts` 提供：

- `registerComponent()`：注册组件、元数据和属性面板。
- `getComponent()`：根据类型拿组件。
- `getAttrComponent()`：拿传统 `Attr.vue`。
- `getComponentMeta()`：拿元数据。
- `getPublicComponents()`：拿物料面板展示组件。
- `isContainer()`：判断是否容器。

`src/custom-component/index.ts` 使用 `defineAsyncComponent` 注册所有物料，并为简单组件配置 `propConfigs`。`VTable` 和 `VChart` 使用复杂 `Attr.vue`，不走简单元数据表单。

### 6.3 属性面板可扩展点

`PropPanelRenderer.vue` 的渲染逻辑：

1. 总是渲染 `CommonAttr` 通用样式。
2. 如果组件元数据有 `propConfigs`，根据配置自动生成属性表单。
3. 如果没有 `propConfigs`，但有 `Attr.vue`，则回退到传统自定义属性面板。

支持控件：`input`、`textarea`、`number`、`color`、`select`、`switch`。

面试表达：

> 新增简单组件不一定要写完整属性面板，只要在注册时声明 `propConfigs`，属性面板就能根据 `style.color`、`propValue.flip.horizontal` 这种路径自动读写嵌套属性。

---

## 7. 拖拽添加组件流程

入口：`src/composables/useDragDrop.ts`。

```text
ComponentList 拖拽
  → dataTransfer 写 index
  → Home.vue 画布 @drop=handleDrop
  → deepCopy(componentList[index])
  → 用 editor.getBoundingClientRect() 计算落点
  → findContainerAt() 判断是否落在容器内
  → 设置 top/left/parentId
  → generateID()
  → changeComponentSizeWithScale()
  → addComponentWithCommand(new AddComponentCommand)
```

容器逻辑：

- `findContainerAt(x, y)` 倒序遍历组件，优先命中最上层组件。
- 跳过锁定组件。
- 只检测 `isContainer(component.component)` 为 true 的组件。
- 命中容器后，子组件坐标变成相对容器坐标，并设置 `parentId = container.id`。

面试表达：

> 拖拽添加不是创建 DOM，而是复制组件模板、生成 id、计算坐标，再通过 `AddComponentCommand` 写入组件数据。视图由 `componentData` 响应式驱动。

---

## 8. 编辑态渲染流程

`Editor/index.vue` 只渲染根级组件：

```ts
componentData.value.filter(c => !c.parentId)
```

子组件由 `NodeRenderer.vue` 递归查找：

```ts
componentData.value.filter(c => c.parentId === props.node.id)
```

渲染结构：

```text
Shape（选中框/拖动/缩放/旋转）
  └─ 动态 component（VText / Picture / RectShape / ...）
       └─ 子 NodeRenderer（如果有 children）
```

性能点：`Editor/index.vue` 在根组件超过 20 个时会使用 `isInViewport` 做简单视口裁剪，并留 `buffer: 200`。

---

## 9. 选中、移动、缩放、旋转

### 9.1 选中

`Shape.vue` 点击组件后：

- `store.setInEditorStatus(true)`，让快捷键生效。
- `store.setClickComponentStatus(true)`，防止误取消选中。
- `store.setCurComponent({ component, index })`。
- 如果右侧面板关闭，则打开右侧面板。

### 9.2 移动

```text
mousedown
  → 记录 oldStyle(top/left)
  → mousemove 中直接给 DOM transform 做即时反馈
  → RAF 节流 store.setShapeStyle({ top, left })
  → editorCtx.startMove() 触发标线
mouseup
  → 清空 transform
  → moveComponent(id, oldStyle, newStyle)
  → CommandManager 执行 MoveCommand
```

优化点：拖动中高频更新用 `createRAFThrottle`，松手后才提交命令，避免每一帧进入撤销栈。

### 9.3 缩放

普通组件有 8 个缩放点，`LineShape` 只有左右两个点。缩放时调用 `calculateComponentPositionAndSize()` 计算新尺寸和位置，RAF 节流写 Store，mouseup 后执行 `ResizeCommand`。

如果组件是 `Group`，且子组件存在非 0/90/180/360 度旋转，会锁定宽高比，避免组合内部组件被异常拉伸。

### 9.4 旋转

旋转按钮拖动时通过 `atan2` 计算鼠标相对组件中心的角度变化：

```text
newRotate = startRotate + rotateAfter - rotateBefore
```

mousemove 中 RAF 节流写 `store.setShapeStyle({ rotate })`，mouseup 后执行 `RotateCommand`。

---

## 10. 框选、组合、拆分

### 10.1 框选

`Editor/index.vue` 画布 mousedown 后显示 `Area` 选择框，mousemove 更新宽高，mouseup 时调用 `getSelectArea()`。它会遍历未锁定组件，用 `getComponentRotatedStyle()` 计算旋转后的包围盒，只有完全落在选择区域内的组件才会被选中。

### 10.2 组合 `ComposeCommand`

真实流程：

1. 从 `store.areaData.components` 取组件 id。
2. 保存原组件快照和原索引。
3. 计算所有组件的最小 `top/left` 和最大 `right/bottom`，得到组合外框。
4. 创建内部 `Group` 组件，`propValue` 保存子组件快照数组。
5. 删除原组件，插入 Group，并选中 Group。
6. `undo()` 删除 Group，并按原索引恢复原组件。

### 10.3 拆分 `DecomposeCommand`

真实流程：

1. 当前组件必须是 `Group`。
2. 保存 Group 快照和原索引。
3. 首次执行时根据 `editor.getBoundingClientRect()` 和父级样式计算子组件绝对位置。
4. 插入拆分后的子组件。
5. 删除 Group。
6. `undo()` 删除拆出的子组件，再恢复 Group。

关键点：拆分依赖 DOM 测量，所以命令会把计算后的 `subComponents` 写入 `data`，便于跨会话恢复时不再重新测量。

---

## 11. 右键菜单、图层、锁定

右键菜单由 `Editor/index.vue` 计算位置，再由 `ContextMenu.vue` 渲染菜单。支持复制、粘贴、剪切、删除、锁定、解锁、置顶、置底、上移、下移。

图层命令在 `LayerCommand.ts`，实际移动的是 `componentData` 数组索引：

| 操作 | 行为 |
| --- | --- |
| 上移 | 当前索引 + 1 |
| 下移 | 当前索引 - 1 |
| 置顶 | 移到数组末尾 |
| 置底 | 移到数组开头 |

锁定通过 `store.lock()` / `store.unlock()` 修改 `curComponent.isLock`。锁定组件不显示缩放点和旋转按钮，也不能被拖动、框选、删除、剪切、组合。

---

## 12. 命令模式与撤销重做

### 12.1 设计

每个操作封装为命令对象，实现：

```ts
execute()
undo()
redo()
serialize()
canMergeWith()
merge()
```

`CommandManager.ts` 使用双栈：

- `undoStack` 保存已执行命令。
- `redoStack` 保存已撤销命令。
- `execute()` 执行命令并压入 undoStack，清空 redoStack。
- `undo()` 弹出 undoStack，执行 undo，再压入 redoStack。
- `redo()` 弹出 redoStack，执行 redo，再压回 undoStack。
- `maxStackSize` 默认 50，`mergeTimeWindow` 默认 300ms。

### 12.2 命令合并

可合并命令：`MoveCommand`、`ResizeCommand`、`RotateCommand`。

合并条件：

- 两个命令都 `mergeable = true`。
- 时间差不超过 300ms。
- 同一组件、同一命令类型。
- 合并后保留第一次的 oldStyle 和最后一次的 newStyle。

### 12.3 命令清单

| 命令 | 文件 | 是否合并 | 用途 |
| --- | --- | --- | --- |
| `ADD_COMPONENT` | `AddComponentCommand.ts` | 否 | 添加组件 |
| `DELETE_COMPONENT` | `DeleteComponentCommand.ts` | 否 | 删除组件 |
| `MOVE_COMPONENT` | `MoveCommand.ts` | 是 | 移动组件 |
| `RESIZE_COMPONENT` | `ResizeCommand.ts` | 是 | 缩放组件 |
| `ROTATE_COMPONENT` | `RotateCommand.ts` | 是 | 旋转组件 |
| `LAYER_*` | `LayerCommand.ts` | 否 | 图层移动 |
| `COMPOSE` | `ComposeCommand.ts` | 否 | 组合 |
| `DECOMPOSE` | `DecomposeCommand.ts` | 否 | 拆分组合 |
| `CLEAR_CANVAS` | `ClearCanvasCommand.ts` | 否 | 清空画布 |
| `IMPORT_DATA` | `ImportDataCommand.ts` | 否 | 导入/恢复数据 |
| `PASTE` | `PasteCommand.ts` | 否 | 粘贴 |
| `CUT_COMPONENT` | `CutCommand.ts` | 否 | 剪切 |
| `BATCH` | `BatchCommand.ts` | 否 | 批量命令 |

### 12.4 序列化

命令通过 `CommandEnvelope` 序列化为纯 JSON：

```ts
{
  id: string
  type: CommandType
  description?: string
  timestamp: number
  data: Record<string, unknown>
}
```

`registry.ts` 保存命令类型到工厂函数的映射，`registerAllCommands()` 负责显式加载所有命令文件。

---

## 13. 保存、恢复、导入导出、版本

### 13.1 自动保存

`useAutoSave.ts`：

- 浅监听 `dataVersion`。
- 防抖 3 秒写入 localStorage。
- 每 60 秒兜底保存。
- `beforeunload` 强制保存。
- 协同开启时跳过 localStorage，由 `y-indexeddb` 持久化 Yjs 文档。

### 13.2 手动保存

`Toolbar.vue`：

- `/editor/:id` 模式调用 `pagesApi.update(id, { componentData, canvasStyle })`。
- 没有 `pageId` 时保存到 localStorage。
- 服务器保存失败时也回退到 localStorage。

### 13.3 恢复

`Home.vue`：

- `/editor/:id` 从服务器加载。
- `/` 从 localStorage 加载。
- 加载前用 Zod 校验组件数据和画布样式。

### 13.4 JSON 导入导出

导出格式：

```json
{
  "version": "1.0.0",
  "timestamp": 1234567890,
  "canvasStyle": {},
  "componentData": []
}
```

导入支持旧格式（组件数组）和新格式（含 `componentData`）。导入时会重新生成组件 id，避免冲突，并通过 `ImportDataCommand` 执行，所以导入可撤销。

### 13.5 版本管理

`useVersionManager.ts`：

```ts
interface PageVersion {
  id: string
  name: string
  description: string
  snapshot: ComponentData[]
  createdAt: string
  thumbnail?: string
}
```

版本保存到 `localStorage.pageVersions`。恢复版本时走 `ImportDataCommand`，因此可撤销。

命令历史和版本管理区别：命令历史是操作级，版本管理是页面快照级。

---

## 14. 预览、截图、HTML 导出

### 14.1 iframe 预览

`Preview.vue` 渲染 iframe `/preview`，iframe load 后通过 `postMessage` 发送 `componentData` 和 `canvasStyle`。`PreviewPage.vue` 校验 `origin` 和 `source` 后，用 `PreviewNodeRenderer` 递归渲染运行态页面。

优点：运行态和编辑态隔离，避免选中框、快捷键、辅助线污染预览。

### 14.2 截图

截图模式下 `Preview.vue` 直接渲染一份深拷贝画布，用 `html-to-image` 的 `toPng(canvas)` 生成图片并下载。

### 14.3 HTML 导出

`exportHtml.ts` 将页面导出为独立 HTML：

- 样式内联，绝对定位。
- 内置 animate.css 子集。
- 支持 `parentId` 子组件递归渲染。
- 事件用 `data-event-type` / `data-event-param`，避免内联 JS 注入。
- 文本和参数用 `escapeHtml`。
- 图片 URL、CSS 颜色有简单校验。

限制：`VChart` 导出时降级为占位，因为独立 HTML 不打包 ECharts。

---

## 15. 运行态能力

### 15.1 动画

`AnimationList.vue` 管理配置，Store 中保存到 `curComponent.animations`。编辑态 `Shape.vue` 可调用 `runAnimation()` 预览动画，运行态和 HTML 导出也会执行进入动画。

### 15.2 事件

`src/utils/events.ts` 当前支持：

- `redirect(url)`：安全校验后打开新窗口。
- `alert(msg)`：浏览器弹窗。

运行态由 `PreviewNodeRenderer.vue` 点击组件时触发。

### 15.3 联动

`useOnEvent.ts` 监听 `eventBus` 的 `v-click` 和 `v-hover`。当触发源 id 和配置匹配时，会修改当前组件样式，并用 `duration` 设置 CSS transition。

### 15.4 数据请求

`request.ts` 支持 GET/POST/PUT/DELETE，一次性请求或定时轮询，返回取消函数。当前实际使用在 `VTable/Component.vue`：组件 mounted 时读取 `props.request`，把接口返回写入 `propValue.data`。

---

## 16. 协同编辑架构

### 16.1 开启方式

- URL 带 `?collab=1`：`main.ts` 自动初始化。
- 工具栏点击“协同编辑”：`Toolbar.vue` 调用 `store.initCommandContext()` 和 `initCollab()`。

房间名通过 `?room=xxx` 指定，否则默认 `default`。

### 16.2 Yjs 文档结构

`src/collab/yDoc.ts`：

```text
Y.Doc
  ├─ yComponents: Y.Array<Y.Map>   对应 componentData[]
  └─ yCanvas: Y.Map                对应 canvasStyleData
```

组件字段中：

- 标量字段直接写入 Y.Map。
- `style` 用嵌套 Y.Map，支持属性级合并。
- `propValue` 整体 JSON 存储。
- `animations/events` 低频变更，整体重建。
- `linkage/request/groupStyle` 整体写入。

面试表达：

> style 是拖拽、缩放、改色等高频并发修改源，所以用属性级 Y.Map；propValue 是 union 类型，可能包含 Group 嵌套数组，当前整体 JSON 存储，降低复杂度。

### 16.3 Pinia 与 Yjs 桥接

`useCollabStore.ts`：

- 创建 `CollabDoc`、Provider、Awareness。
- 监听 `yComponents.observeDeep()` 和 `yCanvas.observe()`。
- Yjs 变更后用 `splice` 替换 `store.componentData` 内容，保留数组引用。
- 同步 `canvasStyleData`。
- 刷新 `curComponent` 引用。
- `applyLocalChange(fn)` 把本地变更包进 Yjs transaction。
- `isApplyingRemote()` 避免远端变更写回 Store 时再次回写 Yjs。

### 16.4 Awareness

`awareness.ts` 管理临时协同状态：

```ts
{
  user: { name, color },
  cursor: { x, y } | null,
  selection: string[]
}
```

它不持久化，用于在线用户、远端光标和远端选区展示。

### 16.5 后端协同服务

`server/yjs-ws.js` 维护：

```text
roomName -> { doc: Y.Doc, awareness: Awareness, connections: Set<ws> }
```

它处理 Yjs sync 消息和 awareness 消息，并转发给同房间其他连接。最后一个连接关闭时销毁房间文档。

---

## 17. 后端 API 与登录页面管理

### 17.1 认证

前端：

- `auth.ts` 保存 `token` 和 `user` 到 localStorage。
- `api.ts` 请求拦截器自动加 `Authorization: Bearer <token>`。
- 401 时清除 token/user。

后端：

- `POST /api/auth/register`：校验、bcrypt 加密、签发 JWT。
- `POST /api/auth/login`：支持用户名或邮箱登录，bcrypt 校验后签发 JWT。
- `authMiddleware`：解析 Bearer token，写入 `req.userId`。

### 17.2 页面 CRUD

| 前端方法 | 后端接口 | 说明 |
| --- | --- | --- |
| `pagesApi.list()` | `GET /api/pages` | 当前用户页面列表 |
| `pagesApi.get(id)` | `GET /api/pages/:id` | 页面详情 |
| `pagesApi.create(data)` | `POST /api/pages` | 创建页面 |
| `pagesApi.update(id, data)` | `PUT /api/pages/:id` | 更新页面 |
| `pagesApi.delete(id)` | `DELETE /api/pages/:id` | 删除页面 |
| `pagesApi.share(id)` | `POST /api/pages/:id/share` | 生成分享 token |
| `pagesApi.unshare(id)` | `DELETE /api/pages/:id/share` | 取消分享 |

`Page` 模型包含 `title/description/userId/componentData/canvasStyle/shareToken/isPublic/createdAt/updatedAt`。

分享页面接口：`GET /api/shared/:token`，无需认证。

---

## 18. 快捷键

`src/utils/shortcutKey.ts` 只有在 `store.isInEditor === true` 时生效。

| 快捷键 | 功能 |
| --- | --- |
| `Delete` / `Backspace` | 删除当前组件 |
| `Ctrl/Cmd + Z` | 撤销 |
| `Ctrl/Cmd + Y` | 重做 |
| `Ctrl/Cmd + C` | 复制 |
| `Ctrl/Cmd + V` | 粘贴 |
| `Ctrl/Cmd + X` | 剪切 |
| `Ctrl/Cmd + G` | 组合 |
| `Ctrl/Cmd + B` | 拆分组合 |
| `Ctrl/Cmd + S` | 保存 |
| `Ctrl/Cmd + P` | 预览 |
| `Ctrl/Cmd + E` | 清空画布 |
| `Ctrl/Cmd + L` | 锁定 |
| `Ctrl/Cmd + U` | 解锁 |

---

## 19. 性能与安全

### 19.1 性能点

| 优化 | 实现 | 价值 |
| --- | --- | --- |
| RAF 节流 | `createRAFThrottle` | 降低拖拽、缩放、旋转、协同光标高频更新 |
| 脏标记保存 | `dataVersion` | 避免 deep watch 整个组件树 |
| 视口裁剪 | `visibleComponents` | 根组件多时减少渲染 |
| 命令合并 | Move/Resize/Rotate | 撤销栈更符合用户心智 |
| 异步组件 | `defineAsyncComponent` | 降低初始加载压力 |
| iframe 预览 | `/preview` | 隔离编辑态和运行态 |

### 19.2 安全点

| 风险 | 处理 |
| --- | --- |
| 文本 XSS | `VText` 使用 DOMPurify |
| HTML 导出注入 | `escapeHtml` 转义 |
| 跳转事件 | 限制 http/https/相对路径 |
| 图片 URL | 校验 `http(s)` 或 `data:image` |
| JSON 坏数据 | Zod 校验 |
| 登录鉴权 | JWT + 路由守卫 + 后端中间件 |

---

## 20. 当前代码风险与优化建议

这些是按真实源码核对后建议面试前关注的点：

1. **命令上下文默认只在协同开启时注入**
   `main.ts` 只有 `?collab=1` 时调用 `initCommandContext()`，但很多单机编辑操作也走命令系统。建议在 `main.ts` 无条件先初始化命令上下文，再按需初始化协同。

2. **`Shape.vue` 文件头部疑似存在异常残留**
   当前文件开头在 `<template>` 前出现异常文本，可能影响 SFC 编译。建议优先运行 `npm run build` 或 `npm run type-check` 验证并清理。

3. **图层策略存在数组顺序和 `zIndex` 双轨**
   渲染使用 `zIndex`，但 `LayerCommand` 移动数组索引，`AddComponentCommand` 不补 `zIndex`。建议统一为数组顺序或统一维护连续 `zIndex`。

4. **预览动画 ref 未绑定风险**
   `PreviewNodeRenderer.vue` 定义了 `wrapperRef`，但模板根节点没有绑定 `ref="wrapperRef"`，运行态动画挂载可能不稳定。

5. **API 401 跳转与 history 路由不一致**
   路由使用 `createWebHistory()`，但 `api.ts` 401 跳转写成 `/#/login`。建议改为 `/login` 或统一用 router 跳转。

6. **旧版 `OnEvent.vue` 保留 Vue2 风格 eventBus API**
   当前主流程使用 `useOnEvent.ts`，旧组件可迁移或删除，避免维护误解。

---

## 21. 面试高频问答

### Q1：项目核心数据结构是什么？

答：

> 核心是 `componentData`。每个组件都包含类型、属性值、样式、父子关系、动画、事件、联动和请求配置。编辑、保存、预览、导出和协同都围绕这份数据进行。

### Q2：为什么不用保存 DOM？

答：

> DOM 是运行结果，不适合作为低代码源数据。低代码需要可编辑、可存储、可导入导出、可撤销、可协同，所以更适合保存 JSON/DSL，再由渲染器把 JSON 转成 DOM。

### Q3：撤销重做怎么实现？

答：

> 用命令模式。每个用户操作是一个命令，实现 `execute/undo/redo/serialize`。`CommandManager` 用 undo/redo 双栈管理历史，移动、缩放、旋转支持 300ms 合并，避免拖拽过程产生大量历史记录。

### Q4：组合组件怎么实现？

答：

> 先框选多个组件，计算包围盒，创建内部 `Group` 组件，把子组件快照放进 `Group.propValue`，删除原组件并插入 Group。撤销时删除 Group，再按原索引恢复原组件。拆分则反向处理，并根据 DOM 测量计算子组件绝对位置。

### Q5：协同编辑怎么避免覆盖？

答：

> 用 Yjs CRDT。`componentData` 映射为 `Y.Array<Y.Map>`，其中高频并发字段 `style` 用嵌套 Y.Map 做属性级合并。Pinia 是本地响应式视图，Yjs 是协同事实源。

### Q6：Awareness 和 Yjs 文档区别？

答：

> Yjs 文档保存页面数据，是可持久化、可合并的；Awareness 保存在线用户、光标和选区，是临时软状态，不持久化，断线后自动清理。

### Q7：iframe 预览有什么好处？

答：

> iframe 可以隔离编辑态和运行态，避免选中框、快捷键、辅助线和编辑器样式影响预览。数据通过 postMessage 从编辑器传给预览页。

### Q8：性能怎么优化？

答：

> 高频交互用 RAF 节流，拖动中先用 transform 做即时反馈，松手后才提交命令；自动保存用 `dataVersion` 脏标记替代 deep watch；组件多时做简单视口裁剪；移动缩放旋转做命令合并。

### Q9：新增组件要改哪些地方？

答：

> 新增组件目录和默认模板，在注册时提供元数据和可选 `propConfigs`。渲染器通过动态 component 渲染，属性面板通过注册表拿元数据自动生成表单。复杂组件再写自定义 `Attr.vue`。

### Q10：生产化还差什么？

答：

> 可以补页面发布/回滚、远程组件市场、响应式布局、权限体系、错误监控、e2e 测试，并修复当前单机命令上下文、图层策略和构建风险点。

---

## 22. 口述模板

### 30 秒

> 这是一个 Vue3 低代码页面搭建平台，用户可以拖拽组件到画布，配置样式、动画、事件和数据请求。项目核心是用 `componentData` JSON 描述页面，通过动态组件递归渲染。为了支持撤销重做，我实现了命令模式；为了支持多人编辑，我接入了 Yjs 协同和 Awareness 在线状态。

### 60 秒

> 项目采用 Vue3 + TypeScript + Pinia + Element Plus，编辑器分为左侧物料区、中间画布和右侧属性面板。所有组件都抽象成 `ComponentData`，包含组件类型、属性值、样式、父子关系、动画、事件和联动配置。用户操作不会直接操作 DOM，而是通过命令模式修改数据，比如 Add、Move、Resize、Compose、Import 等命令，再由渲染器把数据映射成组件。项目还支持 iframe 预览、HTML 导出、版本快照、登录后的页面管理，以及基于 Yjs 的协同编辑。

### 120 秒

> 我做的低代码平台核心是数据驱动编辑器。页面不是保存 DOM，而是保存 `componentData + canvasStyleData`。左侧组件模板通过注册表注册，拖拽时复制模板、生成 id、计算落点，如果拖入容器则设置 `parentId`，然后通过 AddCommand 写入数据。画布通过 NodeRenderer 递归渲染根组件和子组件，外层 Shape 负责选中、拖动、缩放和旋转。
>
> 在工程设计上，我把撤销重做抽象成命令模式，CommandManager 用 undo/redo 双栈管理历史，移动缩放旋转支持 300ms 内合并。组合功能会计算多个组件的包围盒，创建内部 Group 组件；拆分时再恢复子组件绝对位置。保存方面，项目用 `dataVersion` 做脏标记，避免 deep watch 高频触发；JSON 导入和版本恢复都用 Zod 校验并走 ImportCommand，所以也能撤销。
>
> 运行态方面，项目支持动画、点击事件、组件联动、表格数据请求、iframe 预览、截图和独立 HTML 导出。协同方面，我用 Yjs 把组件数据映射成 `Y.Array<Y.Map>`，style 用属性级 Y.Map 合并，Awareness 负责在线用户、光标和选区，WebSocket 负责实时同步，IndexedDB 负责离线持久化。

---

## 23. 文件速查

| 文件 | 作用 |
| --- | --- |
| `src/main.ts` | 前端入口、命令注册、自定义组件安装、可选协同 |
| `src/router/index.ts` | 路由和登录守卫 |
| `src/views/Home.vue` | 主编辑器页面 |
| `src/components/Toolbar.vue` | 顶部工具栏 |
| `src/components/Editor/index.vue` | 画布容器、框选、右键、远端光标 |
| `src/components/Editor/NodeRenderer.vue` | 编辑态递归渲染 |
| `src/components/Editor/Shape.vue` | 选中、移动、缩放、旋转 |
| `src/store/index.ts` | 编辑器主状态 |
| `src/composables/useDragDrop.ts` | 拖拽添加组件 |
| `src/composables/useCommandActions.ts` | 命令操作入口 |
| `src/composables/useAutoSave.ts` | 自动保存 |
| `src/commands/CommandManager.ts` | 撤销重做管理器 |
| `src/commands/*Command.ts` | 各命令实现 |
| `src/custom-component/registry.ts` | 组件注册表 |
| `src/custom-component/component-list.ts` | 物料模板 |
| `src/custom-component/PropPanelRenderer.vue` | 元数据属性面板 |
| `src/collab/yDoc.ts` | Yjs 文档结构 |
| `src/collab/useCollabStore.ts` | Yjs 与 Pinia 桥接 |
| `src/collab/awareness.ts` | 在线用户、光标、选区 |
| `src/utils/exportHtml.ts` | HTML 导出 |
| `src/utils/validation.ts` | Zod 校验封装 |
| `src/utils/sanitize.ts` | 安全工具 |
| `server/app.js` | Express API + Yjs WS 统一入口 |
| `server/yjs-ws.js` | Yjs WebSocket 服务 |
| `server/routes/auth.js` | 注册登录 |
| `server/routes/pages.js` | 页面 CRUD 和分享 |

---

## 24. 复习优先级

必须熟练：

1. `componentData` 数据结构。
2. 拖拽添加流程。
3. `Shape` 移动、缩放、旋转如何写 Store 和命令。
4. CommandManager 双栈和命令合并。
5. 组合、拆分算法。
6. 组件注册表和属性面板元数据。

建议掌握：

1. 自动保存、手动保存、版本管理区别。
2. iframe 预览和 HTML 导出流程。
3. Zod 校验、DOMPurify、安全跳转。
4. 后端 JWT + 页面 CRUD。

加分项：

1. Yjs 文档结构和属性级合并。
2. Awareness 在线光标和选区。
3. IndexedDB 离线持久化和命令历史持久化。
4. 当前代码风险点和优化方案。

---

## 25. 最终总结

这个项目可以按三层记：

1. **编辑器层**：物料拖拽、画布渲染、属性编辑、框选组合、快捷键。
2. **数据与命令层**：`componentData`、Pinia、CommandManager、Zod、localStorage/版本。
3. **运行与协同层**：iframe 预览、HTML 导出、事件联动、Yjs 协同、后端页面管理。

面试最重要的表达：

> 我把页面抽象成一份可序列化、可校验、可协同、可导出的组件 DSL，并围绕这份 DSL 设计了编辑态、运行态、命令历史、组件扩展和多人协同能力。
